# Tasklist — Project SISKA Enhancement (Zammad)

**Basis:** [Gap_Analysis_SISKA_Requirements.xlsx](docs/Gap_Analysis_SISKA_Requirements.xlsx) & [Gap_Analysis_SISKA_Sintesis.md](docs/Gap_Analysis_SISKA_Sintesis.md)
**Urutan prioritas:** 7 → 8 → 3/12 → 1 → 5 → 6 (risiko/effort rendah → tinggi)
**Repo:** [alidjator/zammad-siska](https://github.com/alidjator/zammad-siska)

---

## Fase 0 — Kesiapan Infrastruktur

- [x] Setup repo GitHub + SSH deploy key
- [x] Push baseline source Zammad 7.1.3
- [x] **Bereskan disk host** — free space naik dari ±7.6GB → ±19GB (hapus `/home/pydev/paddle-ocr`, `/home/pydev/paddlex`, clear cache nginx `/var/lib/nginx/.cache`). Tetap monitor saat masuk fase reporting/Elasticsearch (item 7/8).
- [x] Tentukan branching strategy (`main` = baseline, `feature/<no>-<nama-item>` per item gap analysis)
- [x] Keputusan: iterasi langsung di `zammad-staging` yang sudah ada (tidak bikin environment dev terpisah). Konsekuensi: setiap perubahan branch fitur yang di-deploy langsung memengaruhi instance yang dipakai testing/demo — pastikan smoke test & langkah rollback (lihat DEVELOPMENT_WORKFLOW.md) selalu dijalankan tiap deploy.
- [x] Definisikan alur build & deploy: edit source → `docker compose build` → redeploy container → smoke test

---

## Fase 1 — Item No. 7: Reporting Terintegrasi (Low-Medium effort)

- [x] **Bug ditemukan**: kolom `first_response_at`/`close_at`/`last_contact_at` bergeser ~7 jam untuk mayoritas tiket historis (2020-2025) — lihat `docs/BUG_REPORT_TIMEZONE_FIRST_RESPONSE.md`. Dikonfirmasi bukan berasal dari kode 7.1.3 saat ini
- [x] **Root cause dikonfirmasi SEPENUHNYA (2026-09-16)**: investigasi langsung ke server Zammad 3.4.0 production (SSH + rails console + DB, semua read-only) menemukan penyebab pastinya — kolom `tickets.first_response_at`/`close_at`/`last_contact_at` bertipe MySQL **`TIMESTAMP`** (otomatis dikonversi berdasar timezone sesi = WIB), sedangkan `created_at` bertipe **`DATETIME`** (tanpa semantik timezone) dan data `preferences` tersimpan sebagai JSON (juga tanpa semantik timezone) — keduanya karena itu selalu benar. Kesimpulan: **bukan bug di kode Zammad manapun**, melainkan efek samping migrasi data MySQL→PostgreSQL yang tidak menangani ulang konversi timezone khusus untuk kolom bertipe `TIMESTAMP`. Detail lengkap step-by-step investigasi ada di `docs/BUG_REPORT_TIMEZONE_FIRST_RESPONSE.md` poin 7
- [x] **Data historis dikoreksi di staging (2026-09-16)** — `script/fix_timezone_migration_bug.rb` dijalankan setelah dry-run (`SELECT` saja) dan backup penuh (161.884 baris). Hasil: anomali `first_response_at` 97.891→**63**, `close_at` 78.726→**2**, `last_contact_at` 90.357→**0**. Sisa anomali sengaja dilewati (kasus ambigu, ID tersimpan) untuk tinjauan manual terpisah. **Belum dijalankan di production asli** — perlu keputusan & proses persetujuan terpisah untuk itu
- [x] Konfirmasi Elasticsearch aktif & terindeks dengan benar (prasyarat Report Profiles) — 1.19 juta tiket ter-index, cluster health yellow (normal untuk single-node)
- [x] Buat/atur Report Profiles untuk metrik yang dibutuhkan — metric FRT (median, dgn data-integrity filter) + 62 Report Profile (3 Category, 34 Group, 19 Organization) sudah live di staging
- [x] Evaluasi & pilih BI eksternal untuk historis >6.000 baris — **Grafana** dipilih (bukan Kibana). Container `grafana` ditambahkan ke `docker-compose.yml`, role Postgres read-only `grafana_ro`, data source "Zammad Postgres" tersambung, starter dashboard "SISKA - Ticket & CSAT Overview" sudah dibuat
- [x] **Keputusan pivot**: fitur **Feedback Rating/CSAT dibangun native di Zammad** (bukan integrasi/ETL dari sistem eksternal seperti rencana awal) — Object Attributes, Scheduler `Service::Csat::PrepareFeedbackSurveys`, `FeedbackController` publik, Trigger email, pengiriman multi-channel (Email/Telegram/WhatsApp di balik safety-toggle `csat_whatsapp_enabled`), Report adapter `Report::TicketCsatScore`. Lihat `docs/DESIGN_FEEDBACK_RATING.md`
- [x] **Bug ditemukan & diperbaiki**: link rating di pesan Email/Telegram/WhatsApp awalnya tidak menyertakan `&score=`, sehingga link yang dikirim ke customer **selalu gagal** (langsung kena halaman "tidak valid"). Diperbaiki dengan mendesain ulang jadi satu halaman interaktif: bintang 1 baris (klik langsung, tanpa reload) + kolom komentar (`csat_comment`, custom object attribute baru) + tombol kirim tunggal — bukan 5 link terpisah maupun 3 langkah (picker→confirm→submit). Sudah diuji end-to-end (email nyata terkirim ke `jajat.sudrajat@pkp.co.id`, halaman submit berhasil menyimpan skor+komentar)
- [x] Styling halaman feedback disamakan dengan form native Zammad (warna/label/tombol diambil dari `zammad.scss`, bukan tebakan), ditambah logo SISKA (dari `product_logo` Setting via endpoint publik `/api/v1/system_assets/`) dan blockquote pengingat isi keluhan asli tiket (native blockquote style)
- [x] Commit + push perbaikan/redesign `feedback_controller.rb` ke `main`
- [ ] Aktifkan Scheduler CSAT ke live (saat ini `active: false`, menunggu keputusan kapan mulai kirim survey ke customer asli)
- [ ] Konfirmasi format payload WhatsApp gateway dengan tim eksternal — blocked, lihat `docs/WHATSAPP_GATEWAY_REQUIREMENTS.md`
- [ ] Hitung kebutuhan storage untuk retensi historis 2 tahun, sesuaikan dengan kapasitas disk (lihat Fase 0)
- [ ] UAT laporan dengan stakeholder terkait (bandingkan dengan laporan sistem lama)
- [x] **Gap ditemukan & diperbaiki (2026-09-17)**: saat review, grafik native Admin > Reporting > "First Response Time (median minutes)" tampil flat di ~0 untuk hampir semua bulan 2026 dengan profile "-all-". Root cause **bukan** bug timezone migrasi (dicek data September 2026 pasca-cutover, pola sama) — ternyata ~68% tiket (sampel Jan-Ags 2026) adalah tiket **agent-initiated** (mis. log telepon, agent buat tiket sekaligus tulis artikel pertamanya sendiri), sehingga `first_response_at` ≈ `created_at` by construction, bukan cerminan kecepatan respons sungguhan. Dibuktikan lewat `create_article_sender_id`: tiket dengan artikel pertama dari "Customer" median FRT ~25 menit (realistis); dari "Agent" median ~0.01 menit (mendominasi & menenggelamkan sinyal). Dibuatkan Report Profile baru **"FRT: Customer-Initiated Tickets Only"** (`script/create_frt_real_agent_report_profile.rb`, kondisi `ticket.create_article_sender_id = Customer`) — diverifikasi hasilnya bervariasi realistis per bulan (26/27/64/21/50/18/15/7/7 menit, Jan-Sep 2026), dan dikonfirmasi ulang oleh user langsung di browser. Cara pakai: Admin > Reporting > pilih profile ini di panel kiri, bukan "-all-"
- [x] **Ditambahkan metrik Mean berdampingan Median** untuk First Response Time, supaya bisa dibandingkan di grafik yang sama — class backend baru `Report::TicketFirstResponseTimeMean` (`lib/report/ticket_first_response_time_mean.rb`, query & filter integritas data identik dengan versi median, cuma fungsi agregasinya beda), didaftarkan di `app/models/report.rb`. Label grup metrik diubah jadi "First Response Time (minutes)". Perbandingan langsung memperlihatkan Mean jauh lebih tinggi dari Median di hampir semua bulan (mis. Maret: median 64 menit vs mean 729 menit) — indikasi long tail tiket yang direspons sangat lambat. Cara pakai: centang kedua checkbox "Median" dan "Mean" di grup metrik yang sama
- [x] Didokumentasikan lengkap (gap, root cause, perbaikan, penjelasan konsep median vs mean dengan contoh kasus real 9 bulan data live) di `docs/DESIGN_REPORTING_FRT.md`
- [x] **Gap ditemukan & diperbaiki**: tombol "DOWNLOAD ... RECORD(S)" di UI Reporting tidak punya batasan jumlah sama sekali — kombinasi profile "-all-" + rentang multi-tahun sempat berjalan >1:47 menit tanpa selesai (harus dihentikan paksa). Dibuatkan `Report::DownloadLimitGuard` (`lib/report/download_limit_guard.rb`) yang dipasang di ketiga class Report custom kita (`TicketFirstResponseTime`, `TicketFirstResponseTimeMean`, `TicketCsatScore`), dengan Setting baru `report_download_max_records` (default 10.000, Admin > Settings > SISKA > Reporting, `script/create_reporting_settings.rb`). **Bug ditemukan saat implementasi**: `ticket_list.count` polos gagal SQL karena ikut memakai kolom `.select()` yang ada sebagai argumen `COUNT()` — diperbaiki jadi `ticket_list.count(:id)`. Diverifikasi: kondisi melebihi batas diblokir dengan pesan jelas, kondisi dalam batas tetap berjalan normal
- [x] **Diperluas ke class native yang juga rentan**, setelah user menanyakan cakupan proteksi ini. Diperiksa semua class `Report::*` native lain: `TicketGenericTime` sudah aman (limit bawaan via Elasticsearch), `ArticleByTypeSender`/`TicketBacklog` aman (tidak ada loop per-tiket) — tapi **`TicketMoved`, `TicketMerged`, `TicketFirstSolution` ternyata sama rentannya**, dipasangi `Report::DownloadLimitGuard` juga. Diverifikasi lewat pengujian langsung: profile "-all-" + 1 tahun penuh 2026 pada First Solution Time (22.977 tiket) **berhasil diblokir** oleh batas default — bukti nyata mekanismenya bekerja untuk kasus wajar sehari-hari, bukan cuma skenario ekstrem
- [x] **Ditambahkan bypass berbasis permission**, atas permintaan user (kadang ada user bisnis yang sengaja mau unduh besar tanpa peduli performa). Permission baru **`report.unlimited_download`** (`script/create_report_unlimited_download_permission.rb`, sengaja tidak di-assign ke Role manapun otomatis — assign manual lewat Admin > Manage > Roles), dicek di `Report::DownloadLimitGuard.check!(count, user:)` — kalau user punya permission ini, batas dilewati sepenuhnya untuk request itu. Diverifikasi lewat Role & user test sementara (dihapus setelah pengujian): user biasa tetap terblokir (127.001 tiket), user dengan permission berhasil memproses seluruh 127.001 tiket sampai selesai
- [x] **Gap ditemukan & diperbaiki**: mengganti Profile/metric/tahun atau toggle checkbox Median/Mean di UI Reporting tidak menampilkan indikator loading sama sekali (native Zammad, `render()` di `report.coffee` tidak pernah panggil `startLoading`/`stopLoading` untuk `POST /reports/generate`). Diperbaiki dengan parameter `silent` pada `render()` — `false` (default, aksi user) menampilkan loading, `true` (auto-refresh berkala background) tetap diam supaya tidak "berkedip" tiap beberapa menit, mengikuti pola yang sama dipakai `team_kpi.coffee`. Precompile & deploy bersih
- [x] **Bug ditemukan pasca-deploy pertama**: indikator masih belum kelihatan setelah deploy awal — ternyata `startLoading()` bawaan Zammad cuma benar-benar tampil sekali (panggilan pertama per instance controller) kalau dipanggil tanpa target elemen (`el`) eksplisit; constructor `Graph` sudah memanggil render sekali di awal, jadi semua pemanggilan berikutnya jadi no-op diam-diam. Diperbaiki dengan `@startLoading(@$('#placeholder'))` (elemen kontainer chart yang sama dipakai `draw()`). Catatan perilaku native: ada delay anti-flicker bawaan 1800ms — request yang lebih cepat dari itu memang tidak akan menampilkan spinner sama sekali, bukan bug
- [x] **Gap terpisah ditemukan & diperbaiki**: tabel daftar tiket di bawah tombol Download juga tidak ada loading — ternyata dirender class terpisah (`Download#tableUpdate`, AJAX ke `/reports/sets`, sama sekali independen dari `Graph#render` yang sudah diperbaiki). Ditambahkan `startLoading`/`stopLoading` + `error:` callback baru (sebelumnya tidak ada sama sekali — kalau request diblokir `Report::DownloadLimitGuard`, user tidak melihat pesan apa pun). Tidak perlu parameter `silent` di sini karena `tableUpdate()` cuma dipanggil dari aksi user (constructor/selectBackend), auto-refresh berkala sudah di-guard di level `Graph#update`. Diverifikasi format response 422 (`{error: e.message}`) cocok dengan yang dibaca frontend
- [x] **Bug ditemukan pasca-deploy**: user melaporkan "Loading…" tetap macet tampil walau data sudah diterima (`draw()` sudah terpanggil dengan data asli). Ditelusuri ke `App.Delay#set` bawaan Zammad — `startLoading()` memanggil delay tanpa `key` eksplisit, jadi dapat key ACAK tiap panggilan, berpotensi race antara timer 1800ms dan respons AJAX yang membuat `stopLoading()` gagal membersihkan konten yang sudah terlanjur ter-inject. Diperbaiki dengan **berhenti memakai `startLoading`/`stopLoading` sama sekali** di `Graph#render` dan `Download#tableUpdate` — diganti injeksi/pembersihan langsung (`@$('#placeholder').html(...)`/`.empty()`), yang predictable dan self-healing (selalu ditimpa ulang oleh `draw()`/`tableRender()` saat sukses, dikosongkan eksplisit saat gagal). Precompile & deploy bersih
- [x] **Server-side pagination untuk tabel preview** (Section 7, `docs/DESIGN_REPORTING_FRT.md`) — user bertanya apakah tabel bisa server-side, sebelum membangun dilakukan survei empiris dulu ke 189 kombinasi (63 Profile × 3 rentang waktu): **~10% melebihi limit 10.000**, termasuk "-all-" + tahun ini (23.091 tiket) yang kemungkinan besar pilihan paling umum — jadi memang layak dikerjakan, bukan kasus langka. Dipisahkan preview (bisa di-`LIMIT`/`OFFSET`, tidak perlu guard) dari export Excel (`sheet=true`, tetap butuh semua baris, guard TIDAK diubah). Modul baru `Report::ItemsPaginator` dipasang di 4 dari 6 class (`TicketFirstResponseTime`/`Mean`/`TicketCsatScore`/`TicketFirstSolution`) — 2 lainnya (`TicketMoved`/`TicketMerged`, berbasis `history()`) sengaja dilewati (risiko lebih tinggi karena `history()` dipakai bersama `.aggs()`, manfaat lebih kecil karena menghitung event bukan tiket). Reuse `generic/table_pager` lagi di frontend (`Download` class), TAPI kali ini genuinely server-side (tiap klik halaman = request baru), bukan cuma slice array di memori. Diuji langsung lewat Ruby & HTTP asli (profile "-all-" 17.820 tiket: preview 2 halaman berhasil tanpa overlap, jalur sheet=true tetap diblokir guard seperti sebelumnya — sempat kelihatan seperti gagal karena akun test kebetulan sudah punya `report.unlimited_download` dari pengujian sebelumnya, dikonfirmasi ulang pakai `current_user: nil`)
- [x] **Setting untuk ukuran halaman preview** — user tanya apakah per_page bisa dibuat Setting. Ditambahkan `report_preview_per_page` (Admin > Settings > SISKA > Reporting, default 50, `frontend: true`) — dibaca backend (`Report::ItemsPaginator.default_per_page`) DAN frontend (`Download#perPage()`) dari Setting yang sama, bukan 2 angka hardcode terpisah. `MAX_PER_PAGE` (200) sengaja tetap hardcode sebagai pagar keamanan di atas Setting apa pun. Diuji: ubah Setting jadi 20 langsung berefek, `per_page` eksplisit dari client tetap bisa override, Setting diisi 9999 tetap dipangkas ke 200

## Fase 2 — Item No. 8: Dashboard KPI Tim (Medium effort)

- [x] **Keputusan pendekatan**: native ke Zammad sendiri, memanggil API Zammad langsung (bukan embed Grafana/iframe) — dibangun sebagai tab baru "KPI Tim" di Dashboard **frontend legacy** (CoffeeScript/jQuery), karena itu yang aktif dipakai agent sekarang, bukan Vue `/desktop` (dashboard-nya masih di-gate dev-only oleh tim Zammad)
- [x] Finalisasi daftar KPI: FRT (median, rolling window), CSAT (average, rolling window), Tiket New & Open (snapshot realtime), Tiket Escalated (count + rate, realtime)
- [x] Backend: `Service::Dashboard::TeamKpi` + `TeamKpiController`, filter periode rolling 7 hari s/d 2 tahun
- [x] Frontend: tab "KPI Tim", styling native-style via SCSS asli (`dartsass-rails`) — sudut kotak, ikon native-size, grid 3 kartu/baris, warna ikon/angka **state-based** (supergood/good/ok/bad/superbad) mengikuti konvensi native sendiri, bukan warna tetap
- [x] Deploy & smoke test di staging (HTTP 200, precompile bersih, tanpa error log), commit + push ke `feature/08-team-kpi-dashboard`
- [x] Merge `feature/08-team-kpi-dashboard` ke `main`
- [x] **Dokumentasikan & verifikasi endpoint API untuk konsumsi eksternal** — `GET /api/v1/team_kpi?days=N` ternyata bisa dipanggil aplikasi lain di luar Zammad (bukan cuma dari tab dashboard-nya sendiri). Dibuatkan akun service khusus `integration-kpi-api@pkp.co.id` (role "Customer Services", tidak terikat ke satu orang) + Personal Access Token (permission `ticket.agent`), diuji nyata via `curl` dari luar — berhasil, termasuk verifikasi field escalated konsisten di semua rentang `days`. Kontrak API (route, auth, response schema) didokumentasikan lengkap di `docs/DESIGN_TEAM_KPI_DASHBOARD.md` Section 7
- [x] **Auto-refresh berkala** — Setting baru `team_kpi_auto_refresh_seconds` (default 300 detik/5 menit, `0` = mati), diterapkan via `setInterval` di `team_kpi.coffee`. Hanya benar-benar fetch data kalau tab browser aktif DAN sub-tab "KPI Tim" yang sedang dipilih (tidak boros query saat tidak dilihat), dan bersifat silent (tidak flicker loading indicator, tetap tampilkan data terakhir kalau gagal). Deployed & tested di staging
- [x] **Gap ditemukan & diperbaiki**: Setting `team_kpi_auto_refresh_seconds` (dan semua Setting CSAT) awalnya tidak bisa ditemukan lewat Admin > Settings (area custom `CSAT::Base`/`TeamKpi::Base` tidak terdaftar di tab manapun). Dibuatkan tab Admin UI baru **"SISKA"** (`app/assets/javascripts/app/controllers/_manage/siska_settings.coffee`, dengan sub-tab "CSAT" dan "KPI Tim") memakai mekanisme generik `App.SettingsArea` yang sama dengan tab System/Branding bawaan — sekarang semua 6 Setting bisa diatur lewat UI biasa, tidak perlu lagi Rails console/API
- [x] **Bug lain ditemukan & diperbaiki**: semua 6 Setting itu ternyata pakai permission `admin.setting_system` yang **tidak pernah ada** sebagai Permission asli di Zammad (`Permission.where(name: 'admin.setting_system').exists?` → `false`) — diperbaiki ke `admin.system` (permission asli yang dipakai tab "System" bawaan), di server dan di source scripts
- [x] **Bug ketiga ditemukan & diperbaiki**: `csat_feature_launched_at` dibuat tanpa `options.form`, bikin tab "SISKA > CSAT" crash total di browser ("Uncaught No such options.form for csat_feature_launched_at") — diperbaiki dengan menambahkan form definition (input text), diverifikasi ulang oleh user: tab CSAT sekarang tampil lengkap semua field dengan benar
- [x] **Semua nilai kalibrasi jadi Setting** — threshold FRT/CSAT/Escalated (sebelumnya hardcoded di `team_kpi.rb`) + `team_kpi_default_window_days` + `team_kpi_max_window_days` sekarang jadi 5 Setting baru (`team_kpi_frt_thresholds`, `team_kpi_csat_thresholds`, `team_kpi_escalated_thresholds`, `team_kpi_default_window_days`, `team_kpi_max_window_days`), semuanya bisa diatur lewat tab "SISKA > KPI Tim" tanpa redeploy. Frontend (`team_kpi.coffee`) juga dibaca dari Setting yang sama (`team_kpi_default_window_days`, via `App.Config`) supaya default dropdown tidak lagi hardcoded terpisah dan berisiko beda dengan backend. Diverifikasi via API: hasil identik dengan sebelum refactor (tidak ada perubahan perilaku, cuma jadi configurable)
- [x] **Ditambahkan Mean berdampingan Median** di kartu "First Response Time", atas permintaan user — konsisten dengan temuan yang sama di Reporting native (`docs/DESIGN_REPORTING_FRT.md`). Method baru `frt_mean_minutes` (`team_kpi.rb`, `AVG(...)` dari populasi tiket yang persis sama dengan median), field baru `frt_mean_minutes` di response API, ditampilkan sebagai angka sekunder kecil (`.team-kpi-value-secondary`, tidak diberi warna status sendiri) di bawah angka Median utama yang tetap mengontrol warna kartu. Diverifikasi lewat backend langsung: window 7 hari (median 0.0 / mean 0.4) dan 365 hari (median 0.0 / mean 219.9) — pola sama seperti Reporting native, median tertekan ke 0 karena mayoritas tiket agent-initiated, mean tetap menunjukkan gambaran realistis
- [x] **Gap ditemukan & diperbaiki**: user melaporkan format informasi di kartu FRT tidak konsisten — angka Median (utama) tidak diberi label inline, sedangkan angka Mean (sekunder) sudah diberi label eksplisit "Mean:", label "median" cuma muncul di caption bawah yang letaknya jauh dari angka yang dimaksud. Diperbaiki: angka utama sekarang juga diberi label eksplisit "Median: X min" (simetris dengan "Mean: Y min"), caption disederhanakan jadi cuma menampilkan periode ("N hari terakhir") tanpa mengulang kata "median"

## Fase 3 — Item No. 3 & 12: Status Eskalasi + Update Status Tiket (Medium effort)

- [x] **Riset teknis selesai** — ditemukan bahwa sebagian besar requirement item No. 12 (validasi urutan transisi status + pembatasan role) ternyata **native lewat Core Workflow**, bukan custom dev seperti dikira gap analysis awal. Lihat `docs/DESIGN_ESCALATION_STATUS.md` untuk pembagian lengkap native vs custom dev, referensi kode, dan 2 keputusan desain yang sudah disepakati (SLA native tetap paralel/tidak dibekukan by default via `ignore_escalation`; transisi status tanpa pembatasan role dulu) — keduanya diatur lewat Admin UI native, bukan Setting custom
- [x] Buat ticket state baru **"In Progress"** dan **"Eskalasi"** via script (kategori `open`) — `script/create_escalation_object_attributes.rb`
- [x] Buat Custom Object Attribute `escalation_started_at` (Ticket, datetime) + `escalation_budget_hours` (Group & Organization, integer nullable) — file yang sama
- [x] Buat Setting `escalation_budget_hours` (default **8 jam kerja**, global) — muncul di Admin > Settings > SISKA > Eskalasi (tab baru ditambahkan ke `siska_settings.coffee`); override per Group/Organisasi lewat field di masing-masing edit screen (Group menang kalau keduanya di-set)
- [x] Buat Trigger yang mengisi `escalation_started_at` (operator `relative`) saat state berubah ke Eskalasi — `script/create_escalation_trigger.rb`
- [x] Buat aturan Core Workflow untuk validasi urutan transisi (Open tidak boleh lompat langsung ke Eskalasi, harus lewat In Progress dulu; Closed tetap bisa dari state manapun) — `script/create_escalation_workflow.rb`
- [x] **2 bug ditemukan & diperbaiki saat implementasi Core Workflow** (keduanya gagal senyap, tanpa error): (1) format `perform` butuh key `'operator'` eksplisit, bukan langsung nama operator sebagai key; (2) nilai state ID harus string bukan integer, karena `Array#-` tidak melakukan type coercion. Ditemukan lewat reproduksi manual step-by-step, bukan dari membaca kode saja. Detail lengkap + pelajaran untuk Core Workflow rule berikutnya ada di `docs/DESIGN_ESCALATION_STATUS.md` Section 6
- [x] **Diverifikasi end-to-end lewat API sungguhan** (bukan Rails console — ditemukan Core Workflow tidak aktif kalau update lewat `ticket.update!` langsung, cuma jalan lewat controller/API asli): Open→Eskalasi langsung berhasil diblokir (HTTP 422), Open→In Progress→Eskalasi berhasil, dan `escalation_started_at` otomatis terisi
- [x] **Keputusan lokasi tampilan**: kombinasi kolom baru di Overview + card baru di dashboard "KPI Tim" (bukan widget Ticket Zone terpisah)
- [x] Buat Custom Object Attribute `escalation_deadline_at` (Ticket, datetime, nilai TETAP bukan sisa-waktu dinamis) — `script/create_escalation_deadline_attribute.rb`
- [x] Build service custom `Service::Escalation::CalculateDeadlines` — menghitung `escalation_deadline_at` = `escalation_started_at` + budget efektif (Group > Organisasi > global), **wajib pakai kalender jam kerja** `calendar.biz` (mekanisme sama dengan SLA native), bukan pengurangan waktu polos — `app/services/service/escalation/calculate_deadlines.rb`
- [x] Daftarkan Scheduler "Eskalasi: calculate escalation deadlines" (tiap 5 menit, aktif langsung) — `script/create_escalation_scheduler.rb`
- [x] **Kolom Overview** `escalation_deadline_at` — ternyata **murni konfigurasi native** (checkbox Attributes di Manage > Overviews), tidak perlu kode tambahan
- [x] **Card KPI Tim baru** "Tiket Breach Eskalasi" (jumlah tiket Eskalasi yang breach dari total aktif, state-based color) — field baru di `team_kpi.rb` + Setting terpisah `team_kpi_eskalasi_breach_thresholds` + update `team_kpi.coffee`/`team_kpi.jst.eco`
- [x] **Bug ditemukan & diperbaiki saat deploy**: kode custom yang dipanggil Scheduler harus di-deploy ke **dua container terpisah** (`zammad-app` dan `zammad-scheduler`, filesystem writable layer masing-masing beda meski image sama) — Scheduler awalnya gagal 11x berturut-turut lalu auto-nonaktif sendiri (perilaku bawaan Zammad) sebelum file di-copy ke container Scheduler juga. Detail di `docs/DESIGN_ESCALATION_STATUS.md` Section 7
- [x] **Diverifikasi end-to-end di staging**: kalkulasi jam-kerja diuji manual (tiket masuk Eskalasi di luar jam kerja → deadline jatuh ke jam kerja berikutnya, sesuai ekspektasi), override Group diuji & di-revert, dan Scheduler dikonfirmasi jalan sendiri lewat siklus pollingnya (`status: "ok"`, tanpa error) setelah deploy ke kedua container
- [ ] **Selaraskan ekspektasi dengan stakeholder**: SLA clock asli Zammad tidak reset — pastikan tidak ada asumsi keliru soal ini di UAT
- [ ] Notifikasi saat breach — belum diimplementasikan (saat ini cukup indikator angka/warna di KPI Tim), bisa ditambahkan nanti kalau dibutuhkan (perlu keputusan bisnis: notifikasi ke siapa, lewat kanal apa)
- [ ] Testing end-to-end alur status + notifikasi terkait (UAT bersama stakeholder)

## Fase 4 — Item No. 1: AUX Status + Auto-distribusi Tiket (Medium-High effort)

- [x] **Riset teknis selesai** — dicek dulu kemungkinan native sebelum implementasi (pola sama seperti Fase 3). Ditemukan: `Setting ticket_auto_assignment` (native, area `Web::Base`, saat ini nonaktif di staging) — tapi sifatnya **reaktif/klaim** (assign saat agent buka tiket dari Overview), bukan push proaktif berbasis status; `Group#assignment_timeout` (native, reclaim tiket basi, bukan distribusi); `out_of_office` (native, granularitas per-hari, cuma 1 pengganti, tidak cukup untuk status granular). **Kesimpulan: inti requirement (status AUX granular + durasi + distribusi proaktif) tetap genuinely custom dev** — beda dari Fase 3 yang mayoritas ternyata native. Detail lengkap di `docs/DESIGN_AUX_STATUS.md`
- [x] **6 pertanyaan terbuka sudah dijawab, keputusan desain final** (Section 4, `docs/DESIGN_AUX_STATUS.md`): (1) status Available/Busy Lunch 30m/Busy Meeting 60m/Busy Training 60m/Offline; (2) routing bisa dipilih lewat Setting antara 3 strategi gaya queue Asterisk PBX (lihat poin di bawah); (3) semua Busy/Offline → tetap unassigned biasa, tanpa eskalasi khusus; (4) mekanisme independen & proaktif, bukan dibangun di atas `ticket_auto_assignment` native; (5) agent ubah status sendiri + supervisor/admin bisa override; (6) histori status disimpan untuk laporan produktivitas nanti. Siap lanjut ke desain teknis rinci (skema data, nama Custom Object Attribute, struktur Scheduler) sebelum implementasi
- [x] **Menambahkan 2 metode routing tambahan** atas permintaan user, jadi total 3 strategi bisa dipilih lewat Setting baru `aux_status_routing_method` (dropdown, tanpa perlu deploy ulang kode): `leastrecent` (default, paling lama tidak dapat tiket), `fewestcalls` (beban kerja tiket open aktif paling sedikit — dikonfirmasi user real-time, bukan histori/total sepanjang waktu), `roundrobin` (gilir tetap berurutan, butuh 1 pointer state tersimpan). Detail di Section 4/2a `docs/DESIGN_AUX_STATUS.md`
- [x] **Desain teknis rinci selesai** (Section 5, `docs/DESIGN_AUX_STATUS.md`) — sudah ditelusuri kode native sebelum menentukan titik integrasi: Custom Object Attribute `User#aux_status`/`aux_status_since`/`aux_status_expires_at`; Setting `aux_status_types` (daftar status+durasi), `aux_status_routing_method`, `aux_status_roundrobin_pointer`; Service Objects `Service::AuxStatus::{ChangeStatus,SelectAgent,DistributeTicket,ExpireStatuses}`; distribusi proaktif lewat mekanisme **Transaction Backend async native** (`Transaction::AuxStatusDistribution`, bukan Trigger — Trigger tidak bisa panggil Ruby custom langsung); Scheduler polling tiap 1 menit untuk auto-expiry; tabel histori custom `AuxStatusLog`; API `Api::V1::AuxStatusesController` + UI dropdown di navigasi personal; permission baru `aux_status.override`. Siap mulai implementasi kode
- [x] **Implementasi backend selesai & terverifikasi di staging** (Section 6, `docs/DESIGN_AUX_STATUS.md`): Custom Object Attribute `User#aux_status`/`aux_status_since`/`aux_status_expires_at` + migration tabel histori `aux_status_logs` (`AuxStatusLog` model); Setting `aux_status_durations` (daftar status VALUES sendiri ternyata tidak perlu Setting terpisah — sudah native lewat opsi `select` Object Manager), `aux_status_routing_method`, `aux_status_roundrobin_pointer`; Service Objects `Service::AuxStatus::{ChangeStatus,SelectAgent,DistributeTicket,ExpireStatuses}`; `Transaction::AuxStatusDistribution` (async, khusus tiket baru — titik "agent jadi Available" ternyata lebih tepat dipanggil langsung dari `ChangeStatus`, bukan lewat Transaction Backend juga); Scheduler polling tiap 1 menit; permission `aux_status.override`; API `PUT /api/v1/aux_status` (+ `/:user_id` untuk override). **1 bug nyata ditemukan & diperbaiki saat pengujian**: distribusi butuh akses Group `'full'`, bukan `'change'` (native `Ticket#check_owner_active` diam-diam membatalkan assignment kalau cuma `'change'`). Diuji end-to-end penuh di Group "QA - Internal Testing" (duration/expiry, histori, validasi, otorisasi, 3 strategi routing, distribusi push & pull, Scheduler expiry) — semua data test sudah dibersihkan
- [x] **Implementasi frontend selesai (backend-verified, UI belum diklik langsung di browser)**: dropdown ganti status cepat di menu personal (avatar, kanan atas) — 5 baris flat (nesting menu personal Zammad cuma 1 tingkat, submenu bertingkat tidak didukung), checkmark di status aktif, plus sub-tab Admin "AUX Status" di Settings > SISKA
- [x] **UI override oleh supervisor selesai** (Section 6d, `docs/DESIGN_AUX_STATUS.md`) — halaman baru Admin > Manage > **AUX Status**, tabel semua agent + dropdown override per baris, dibatasi permission `aux_status.override`. Backend `GET /api/v1/aux_statuses` diuji negatif (token tanpa permission override → 403), jalur "berhasil" belum bisa diuji di sesi ini (lihat 2 poin di bawah)
- [x] **Verifikasi browser sebagian dilakukan user (via screenshot)**: Admin > Settings > SISKA > AUX Status tab tampil & berfungsi normal (durations + routing method). Dropdown status di menu personal tampil sesuai desain (5 baris + header)
- [x] **Bug ditemukan & diperbaiki (teks tidak terlihat)**: user melaporkan "ketika di klik aux, status yang terpilih jadi ga terlihat tulisannya" — dilacak ke CSS native `.dropdown-menu.dropdown-menu--light li.is-active { color: var(--text-inverted) }` (putih tanpa background sesuai di konteks ini = teks putih di atas terang = tidak terlihat). Diperbaiki lewat `containerClass` baru + stylesheet `aux_status.scss` yang menetralkan cuma untuk baris AUX Status sendiri (pola sama seperti `team_kpi.scss`)
- [x] **Bug ditemukan & diperbaiki (divider hilang)**: user melaporkan tidak ada divider antara grup AUX Status dan menu lain (Dark Mode dst.) — ditemukan divider di Zammad selalu dimiliki item SETELAH celah (pola sama seperti `Logout`), Dark Mode sebelumnya tidak butuh divider sendiri sampai grup AUX Status disisipkan tepat sebelumnya. Diperbaiki dengan `divider: true` di registrasi `DarkMode` sendiri (`dark_mode.coffee`)
- [x] **Akar masalah sebenarnya ditemukan & diperbaiki (bukan CSS `.is-active`)**: user mencoba lagi pakai private/incognito browsing (menyingkirkan kemungkinan cache/tab lama) — teks MASIH tidak terlihat. Pola dicek ulang: baris blank selalu yang SEDANG jadi status aktif. Ditelusuri ke `App.Utils.icon()` yang tidak pernah validasi nama ikon — kode kami pakai `iconClass: 'check'`, padahal ikon itu **tidak ada** di sprite Zammad (yang benar `checkmark`). `<use>` yang menunjuk id tak ada + `<svg>` tanpa width/height eksplisit = browser kasih ukuran fallback 300x150px, menutupi teks di baris setinggi 39px. Diperbaiki: ganti `'check'` → `'checkmark'`. **Dikonfirmasi user via screenshot: teks + centang sudah tampil normal**. Detail di Section 6f `docs/DESIGN_AUX_STATUS.md`
- [x] **Fitur baru: layar freeze + popup timer saat status berdurasi aktif** (Section 6h, `docs/DESIGN_AUX_STATUS.md`) — permintaan user, diklarifikasi dulu 2 keputusan (cuma status berdurasi yang trigger freeze; ada tombol "End Break Now" untuk batal lebih awal). Dibangun `App.AuxStatusFreezeWidget` — overlay full-screen (`z-index: 5000`) dengan countdown `mm:ss` dihitung dari sisi client (presisi, tidak menunggu Scheduler backend yang jeda sampai 1 menit — Scheduler tetap jalan sebagai safety net), pola singleton (beda dari dropdown yang dibuat ulang tiap render) supaya countdown tidak reset/dobel, dicek juga di render pertama saat login/reload halaman. Sudah di-deploy, dikonfirmasi masuk ke bundle yang disajikan — **verifikasi interaktif (overlay muncul, countdown jalan, tombol berfungsi) belum dilakukan user**
- [x] **Fitur baru: Setting untuk menambah/mengurangi nilai AUX** (Section 6j, `docs/DESIGN_AUX_STATUS.md`) — permintaan user, merevisi keputusan sebelumnya (yang mengandalkan Object Manager). Setting baru `aux_status_options` (JSON array via textarea di Admin > Settings > SISKA > AUX Status, `frontend: true`) jadi SATU-SATUNYA sumber kebenaran untuk daftar status + label + durasi, menggantikan `aux_status_durations` (di-retire) dan opsi native `select` di Object Manager (`aux_status` diubah jadi `input` biasa). Backend (`ChangeStatus`) dan 3 titik frontend (dropdown personal, freeze overlay, halaman Manage override) semuanya diupdate baca dari Setting ini secara dinamis — termasuk `FREEZE_STATUSES` hardcode di freeze overlay yang DIHAPUS SELURUHNYA, diganti cek murni `aux_status_expires_at` supaya status baru apa pun otomatis kena freeze kalau berdurasi. Diuji end-to-end langsung: status baru "Busy Coaching" (45 menit) ditambahkan murni lewat Setting, langsung diterima tanpa perubahan kode — **verifikasi UI (edit Setting dari Admin, dropdown menyesuaikan) belum dilakukan user**
- [x] **Permission `aux_status.override` dikonfirmasi sudah tersimpan** ke Role "Admin" — dicek ulang langsung ke database (`updated_at` terbaru: 2026-09-17 23:12:57 UTC), user sudah berhasil menyimpan ulang
- [x] **Bug ditemukan & diperbaiki: halaman Manage > AUX Status macet loading selamanya** — user melaporkan macet di "Loading...". Root cause: pola bug yang SAMA PERSIS dengan yang pernah diperbaiki di Reporting (`docs/DESIGN_REPORTING_FRT.md`) tapi terlewat diterapkan di halaman baru ini — `@startLoading()`/`@stopLoading()` bawaan tanpa target elemen eksplisit bisa macet permanen kalau delay anti-flicker 1800ms-nya sempat menyala duluan. Diperbaiki dengan pola yang sama: injeksi/pembersihan langsung ke placeholder spesifik (`.js-aux-status-body`), sukses menimpa ulang, gagal menampilkan pesan error eksplisit (bukan macet kosong). Detail di Section 6k `docs/DESIGN_AUX_STATUS.md`
- [x] **Fitur baru: editor baris untuk Setting AUX Status, menggantikan textarea JSON mentah** (Section 6l, `docs/DESIGN_AUX_STATUS.md`) — permintaan user ("saya mau ... input field bukan dengan JSON, karena semua admin belum tentu tau JSON"). Dipakai mekanisme resmi Zammad `setting.preferences.controller` (dikonfirmasi lewat pembacaan `_settings/area.coffee`, sudah dipakai Setting native lain seperti `App.SettingsAreaSwitch`) untuk mengganti tampilan generik Setting ini dengan tabel input Value/Label/Durasi + tombol Add/Remove/Submit. Format data yang tersimpan di server tidak berubah (tetap JSON array yang sama), cuma cara mengeditnya yang jadi ramah non-teknis. Sudah di-deploy, `preferences.controller` dikonfirmasi tersimpan di database — **verifikasi interaktif belum dilakukan user**
- [x] **Fitur baru: pagination di halaman Manage > AUX Status** (Section 6m, `docs/DESIGN_AUX_STATUS.md`) — permintaan user setelah tabel 513 agent tampil tanpa pagination. Dipakai ULANG partial native `generic/table_pager.jst.eco` (sudah dicek dulu, dipakai juga oleh halaman Admin list lain seperti Roles/Groups) supaya tampilannya identik dengan pagination yang sudah familiar, bukan bikin versi sendiri. Paginasi di sisi client (25 baris/halaman, 21 halaman total) karena data sudah diambil sekali lewat API. Ditemukan & diperbaiki sekalian: class CSS `table--large`/`table--bordered`/`table--striped` yang sempat dipakai ternyata TIDAK PERNAH ada di `zammad.scss` (dicek dulu sebelum dipakai kali ini, pelajaran dari bug ikon `check` sebelumnya) — diganti `table table-hover` yang benar-benar ada. **Verifikasi interaktif (klik nomor halaman) belum dilakukan user**
- [x] **Fitur baru: halaman Manage > AUX Status jadi search-first, bukan load-semua-lalu-paginate** (Section 6o, `docs/DESIGN_AUX_STATUS.md`) — permintaan user demi performa ("saya tidak mau menampilkan data agent, tapi hanya menampilkan pencarian agent, supaya halaman tidak lama loadingnya"). `GET /api/v1/aux_statuses` sekarang wajib parameter `query` — kosong langsung `[]` tanpa sentuh tabel `users` sama sekali (diuji: 0.15 detik), terisi difilter dulu di SQL (`ILIKE` + `limit(100)`) sebelum baru cek permission satu-per-satu ke himpunan yang jauh lebih kecil. Frontend tidak lagi auto-fetch saat dibuka, cuma kotak pencarian + hint, di-debounce 300ms (pola sama seperti search box `Navigation` sendiri), hasil pencarian usang dibuang kalau user sudah mengetik lebih lanjut. Pagination (6m) tetap berlaku di atas hasil pencarian. Diuji end-to-end lewat API langsung (`query=peg` dan `query=hasbiallah`, dua-duanya berhasil menemukan agent yang sama) — **verifikasi interaktif di browser belum dilakukan user**
- [x] **Setting untuk ukuran halaman di Manage > AUX Status** (Section 6q, `docs/DESIGN_AUX_STATUS.md`) — user bertanya dulu apakah Setting `report_preview_per_page` berlaku untuk semua tabel Zammad (dijawab tidak, tiap tabel beda mekanisme), lalu diminta dibuatkan yang serupa di sini. Ditambahkan `aux_status_manage_per_page` (default 25, `frontend: true`) — beda dari Reporting, Setting ini murni client-side (motong array hasil pencarian yang sudah ada di memori, bukan `LIMIT` SQL, karena endpoint pencarian sudah dibatasi `limit(100)` terpisah), jadi cukup 1 method `pageSize()` baru di `_manage/aux_status.coffee` menggantikan konstanta lama
- [x] **Diubah ke server-side pagination sungguhan + bug ditemukan & diperbaiki** (Section 6s, `docs/DESIGN_AUX_STATUS.md`) — bagian dari instruksi "buat semua menjadi server-side ... terlepas datanya besar atau kecil". `page`/`per_page` sekarang menentukan `LIMIT`/`OFFSET` SQL langsung. Ditemukan bug nyata saat pengujian: query luas ("anon", "a") mengembalikan 0 hasil walau agent asli ada, akar masalah pendekatan lama (ambil 100 baris kandidat dulu baru filter permission `ticket.agent` di Ruby) gagal untuk query yang cocok ke sebagian besar tabel `users` (agent cuma ~0,7% dari total). Percobaan `.order(:firstname, :lastname)` saja tidak cukup — perbaikan sesungguhnya: `Permission.join_with` (mekanisme native yang sama dipakai `User::HasGroups#group_access`) mendorong filter permission ke SQL JOIN, bukan loop Ruby setelah LIMIT. Diverifikasi lewat HTTP asli pasca-deploy: query "anon" → 508 agent, query "a" → 513 (semua agent), query "peg" → 1 (regresi aman)
- [ ] Testing lanjutan dengan multi-agent nyata di luar Group QA — sengaja BELUM dilakukan sepihak karena berisiko meng-assign ulang tiket unassigned asli ke agent nyata; perlu keputusan user soal Group & waktu yang aman

## Fase 5 — Item No. 5 & 6: Live Chat Enhancement (High effort, berisiko)

**Item 5 — Auto-create ticket saat chat mulai**
- [x] **Riset kode channel chat selesai** (`docs/DESIGN_LIVE_CHAT_ENHANCEMENT.md`) — titik hook ditemukan: `lib/sessions/event/chat_session_start.rb#run` (saat agent accept chat, state waiting→running). Ditemukan juga: tombol "Turn into ticket" YANG SUDAH ADA (`chat.coffee#ticketCreate`) BUKAN relasi sungguhan — cuma membuka layar New Ticket dengan body pra-isi, tidak ada kolom `ticket_id` di `chat_sessions` sama sekali. **Masalah terbuka ditemukan**: live chat visitor anonim by default (tidak ada email/customer_id tertangkap), Ticket Zammad wajib `customer_id` — perlu keputusan user (buat User placeholder / tunda pembuatan tiket sampai email diketahui / wajibkan pre-chat form) sebelum desain teknis rinci lanjut
- [x] **Keputusan user**: wajibkan form nama+email sebelum chat mulai (opsi paling bersih datanya)
- [x] **Desain teknis rinci selesai** (Section 5.1, `docs/DESIGN_LIVE_CHAT_ENHANCEMENT.md`) — migration `chat_sessions.email`/`ticket_id`; form pra-chat di widget; validasi server-side berlapis di `ChatSessionInit`; resolusi customer PAKAI ULANG `Channel::Filter::BaseIdentifyUser.user_create` (mekanisme native yang sudah teruji jalur email, bukan logika baru); resolusi Group via preferensi per-topik chat atau Setting global baru, dengan fallback aman (skip auto-create, bukan crash) kalau belum dikonfigurasi; sinkron transkrip real-time ke tiket via `chat_session_message`; tombol "Turn into ticket" existing diubah jadi "Open Ticket" kalau tiket sudah ada (hindari duplikasi sesuai catatan tasklist)
- [x] **Keputusan final** (Section 5.1.6): Group default `chat_auto_ticket_group_id` = **"QA - Internal Testing"** untuk pengembangan/pengujian dulu — **WAJIB diarahkan ulang ke Group produksi sebelum dipakai untuk chat customer asli** (kalau dibiarkan, tiket customer asli masuk ke Group tanpa anggota). State/priority tiket baru ikut default sistem, tidak perlu nilai khusus
- [x] **Fitur baru dirancang: riwayat chat sebelumnya dari customer yang sama** (Section 5.1.7) — memanfaatkan email yang sekarang wajib tersimpan (5.1.1/5.1.2): saat agent accept chat, dicari 5 sesi chat terakhir dengan email yang sama (bukan sesi ini), dikirim ke agent lewat payload `chat_session_start` yang sudah ada, ditampilkan di panel meta yang sudah ada di jendela chat (bukan UI baru terpisah) — masing-masing berisi tanggal + link ke tiket kalau ada. Sesi lama sebelum fitur ini aktif tidak akan muncul (tidak ada email tersimpan) — keterbatasan yang disengaja diterima, bukan backfill data lama. **Sengaja hanya sisi AGENT** (dikonfirmasi ulang ke user saat ditanya) — tidak ada di widget customer sama sekali
- [x] **Diperluas atas permintaan user: transkrip LENGKAP per sesi sebelumnya, bukan cuma tanggal+link tiket** — backend `previous_session_summary` sertakan array `messages` (content + `is_from_agent`, logika sama dgn sinkron-ke-tiket); UI dibungkus `<details>`/`<summary>` native HTML per sesi (collapsed by default). Diverifikasi lewat Puppeteer (render template langsung dengan data contoh) — transkrip + label Customer/Agent muncul benar. Lihat entry 100 `docs/ACTIVITY_LOG_SISKA.md`
- [x] **Ditemukan & diperbaiki: riwayat lenyap TOTAL setelah agent reload halaman chat yang masih berjalan** — root cause: `previous_sessions` cuma dihitung sekali di `ChatSessionStart#run` (accept baru), sedangkan jalur reconnect (`Chat::Session.active_chats_by_user_id`, dipicu heartbeat/reload) tidak pernah menyertakannya. Dipusatkan jadi method `Chat::Session#previous_sessions_summary`, dipanggil dari KEDUA jalur. Direproduksi & diverifikasi ulang lewat Puppeteer (`agent.reload()` di tengah chat aktif) — hasil sebelum/sesudah reload sekarang identik. Lihat entry 101 `docs/ACTIVITY_LOG_SISKA.md`
- [x] **Ditambahkan affordance visual (panah expand/collapse) di panel riwayat** — user sempat kira fitur tidak jalan ("masih sama"), ternyata SUDAH bekerja penuh tapi marker `<details>` bawaan nyaris tak terlihat (pola sama seperti bug ikon reply, entry 96). Diganti panah kustom yang berputar saat dibuka. Lihat entry 102 `docs/ACTIVITY_LOG_SISKA.md`
- [x] **Ditambahkan link "Open Ticket" yang terlihat SELAMA chat masih berlangsung** — auto-create tiket TERNYATA sudah bekerja benar sejak awal (dicek ke database), yang hilang cuma tampilannya: tombol bawaan Zammad cuma muncul native SETELAH chat berakhir. Ditambahkan baris "Ticket: Open Ticket" di panel info, muncul selama chat masih aktif. Lihat entry 103 `docs/ACTIVITY_LOG_SISKA.md`
- [x] **Backend diimplementasikan & diverifikasi end-to-end** — migration `chat_sessions.email`/`ticket_id`; `ChatSessionInit` validasi nama+email 2 lapis; `ChatSessionStart` resolusi customer (reuse `Channel::Filter::BaseIdentifyUser.user_create`) + resolusi Group + auto-create tiket + fallback aman + riwayat 5 sesi terakhir; `ChatSessionMessage` sinkron transkrip real-time ke tiket dengan `created_by_id` eksplisit (BUKAN mengandalkan `UserInfo.current_user_id` ambient, yang selalu nil untuk pesan customer anonim). Diuji langsung lewat `rails runner`: visitor sama dikenali sebagai customer yang sama (tidak duplikat), Group belum dikonfigurasi → dilewati aman tanpa crash, riwayat sesi sebelumnya + link tiket ditemukan benar. Semua data uji sudah dibersihkan
- [x] **Frontend PANEL AGENT diimplementasikan** (`chat.coffee`, `customer_chat/*.jst.eco`) — tombol "Turn into ticket" berubah jadi "Open Ticket" kalau `session.ticket_id` sudah ada; riwayat 5 sesi sebelumnya ditampilkan di panel meta (`.js-meta`) dengan link ke tiket. Lolos `assets:precompile` bersih, kode baru dikonfirmasi ada di bundle
- [x] **Frontend WIDGET CUSTOMER (form pra-chat) diimplementasikan untuk KEDUA varian** (`public/assets/chat/chat.coffee` jQuery + `chat-no-jquery.coffee`, toolchain gulp terpisah) — form Nama+Email WAJIB diisi sebelum `chat_session_init` dikirim, validasi client-side + tangani penolakan server (form muncul lagi dengan pesan error, bukan loader macet selamanya). **Ini perbaikan KRITIS**: tanpa ini, live chat customer akan gagal total begitu backend Item 5 aktif (server sekarang menolak `chat_session_init` tanpa nama/email). Build gulp berhasil bersih untuk kedua varian, dikonfirmasi kode baru ada di `chat.js`/`chat.min.js`/`chat-no-jquery.js`/`chat-no-jquery.min.js`
- [x] **Verifikasi browser sungguhan sebagian dilakukan** — dibuatkan akun agent test (`siska.chat.agent@pkp.co.id`) + halaman customer test (`https://helpdesk.satu.solutions/test-chat-customer.html`). Dikonfirmasi: enable/disable attachment per-agent bekerja benar (pesan penolakan muncul sesuai desain saat agent belum mengaktifkan). **Ditemukan & diperbaiki**: jendela chat menempel pas ke tepi bawah layar (`bottom: 0` bawaan Zammad, dikonfirmasi ke user dulu sebelum diubah) — diubah jadi `bottom: 20px` di `chat.scss` + `restingBottom` baru di `chat.coffee` (jQuery) supaya animasi buka/tutup ikut konsisten; varian `chat-no-jquery.coffee` otomatis ikut karena pakai CSS transform relatif
- [x] **Regression test end-to-end reply-to-message via browser sungguhan (Puppeteer) — 2 bug nyata ditemukan & diperbaiki**: (1) payload `reply_to` tidak sampai ke penerima balasan (`chat_session_message.rb` cuma serialize kolom `reply_to_id`, bukan isi pesan yang dirujuk) — diperbaiki dengan payload eksplisit; (2) fix di atas sudah benar di `rails runner` tapi tetap tidak muncul di browser sungguhan — akar masalah baru: `zammad-websocket` adalah CONTAINER TERPISAH dari `zammad-app` (tidak berbagi filesystem), deploy sebelumnya hanya menyentuh `zammad-app`. Ditetapkan disiplin baru: setiap perubahan `lib/sessions/event/chat_*.rb`/model chat WAJIB di-deploy ke KEDUA container. Diverifikasi ulang lewat screenshot browser — kutipan pesan asli sekarang muncul benar di sisi penerima
- [x] **Ditemukan & diperbaiki (laporan user, screenshot beranotasi tangan): ikon reply di widget customer nyaris tidak terlihat + memicu scrollbar horizontal** — bug CSS spesifik widget (`chat.scss`: `opacity:0` hover-only, posisi `-1.6em` DI LUAR bubble), dikontraskan dengan CSS setara sisi agent yang ternyata sudah benar sejak awal. Diperbaiki: posisi ke dalam bubble, opacity selalu terlihat sebagian (0.5, penuh saat hover). Diverifikasi lewat build + screenshot Puppeteer (markup pesan asli disuntik ke widget live) — ikon terlihat jelas, `scrollWidth === clientWidth` (tidak ada overflow lagi)
- [x] **Ditemukan & diperbaiki (laporan user langsung dari sesi pengujian mandiri): ikon reply widget customer masih salah tempat setelah fix sebelumnya (entry 96)** — root cause sesungguhnya: `position: relative` terpasang di elemen WRAPPER LUAR (`.zammad-chat-message`), bukan di bubble yang benar-benar terlihat (`.zammad-chat-message-body`), sehingga ikon (posisi absolut) mengambang relatif ke wrapper lebar-penuh, bukan ke bubble sempit — beda struktur dari sisi agent (di sana bubble = wrapper, jadi tidak kena masalah ini). Diperbaiki: markup ikon dipindah jadi ANAK `.zammad-chat-message-body` (`views/message.eco`) + `position: relative` dipindah ke situ juga (`chat.scss`). Diverifikasi terukur (`getBoundingClientRect()`) ikon 100% di dalam batas bubble, kedua sisi customer & agent — lihat entry 98 `docs/ACTIVITY_LOG_SISKA.md`
- [ ] Regression test langsung oleh USER SENDIRI untuk upload attachment dari widget & reply-to dari sisi customer (alur customer→agent) — sejauh ini reply-to sudah diverifikasi end-to-end oleh saya sendiri (lewat Puppeteer), tapi belum dicoba langsung oleh user di browsernya sendiri

**Item 6 — Attachment di Live Chat**
- [x] **Riset selesai** (`docs/DESIGN_LIVE_CHAT_ENHANCEMENT.md`) — dikonfirmasi `chat_messages` TIDAK punya kolom/relasi attachment apa pun (cuma `content` teks). Model `Store` (dipakai Ticket::Article) bisa dipakai ulang untuk penyimpanan. TAPI cakupan pekerjaan genuinely luas: menyentuh 2 codebase terpisah (Rails app + widget standalone `public/assets/chat/`, toolchain build sendiri, bukan Vite/asset pipeline utama), butuh endpoint upload baru, dan permukaan keamanan baru (upload dari pengunjung anonim)
- [x] **Keputusan user**: tetap lanjut desain teknis rinci meski risiko tinggi
- [x] **Desain teknis rinci selesai** (Section 5.2, `docs/DESIGN_LIVE_CHAT_ENHANCEMENT.md`) — TIDAK butuh migration (Store sudah polimorfik lewat object+o_id, dikonfirmasi dari `CanCloneAttachments` concern yang sudah dipakai Ticket::Article/Knowledge::Base::Answer); endpoint upload BARU (bukan `UploadCachesController` yang sudah ada — itu wajib login, visitor chat anonim), otorisasi berbasis validitas `session_id` bukan token/akun (preseden sudah ada di proyek ini sendiri: halaman feedback CSAT publik Fase 1); integrasi ke tiket (kalau Item 5 aktif) pakai ulang pola `Store.create!` yang sama seperti `clone_attachments`
- [x] **Keputusan final** (Section 5.2.4): whitelist tipe file SELALU aktif (baseline), batas 5 MB/file maksimum 5 file/sesi chat
- [x] **Whitelist tipe file dibuat configurable lewat Setting** (Section 5.2.4a) — atas permintaan user. Setting baru `chat_attachment_allowed_extensions` (teks-koma sederhana, bukan JSON/editor baris seperti aux_status_options — cukup satu daftar datar). Validasi 2 lapis: denylist keras ekstensi berbahaya di KODE (tidak bisa di-override Setting apa pun) dicek lebih dulu, baru Setting dicek untuk ekstensi yang tersisa — mencegah kesalahan konfigurasi admin membuka celah keamanan
- [x] **Batas ukuran upload dibuat configurable lewat Setting** (Section 5.2.2a) — atas permintaan user. Setting baru `chat_attachment_max_size_mb` (default 5), dicek dulu Setting sejenis (`es_attachment_max_size_in_mb`) TERNYATA untuk tujuan berbeda (indexing Elasticsearch, bukan kontrol upload) jadi tidak dipakai ulang. Divalidasi dari `Content-Length` DAN ukuran data aktual (header bisa dipalsukan). Pagar batas atas mutlak tetap hardcode (pola sama seperti `MAX_PER_PAGE` di `Report::ItemsPaginator`) supaya Setting tidak bisa diisi angka sangat besar tanpa kendali
- [x] **Desain integrasi ClamAV plug-and-play** (Section 5.2.5) — user bertanya bisa tidak dipindai ClamAV. Dicek dulu kapasitas server: RAM tersedia cuma ~1,9 GB, disk 96% penuh — memasang ClamAV (`clamd` butuh ~1-1,5 GB RAM) SEKARANG berisiko. Dirancang integrasi dalam kondisi NO-OP by default (2 Setting baru `chat_attachment_clamav_host`/`_port`, kosong = nonaktif), service class `Service::Chat::VirusScan` (protokol INSTREAM ClamAV, fail-CLOSED kalau sudah dikonfigurasi tapi clamd tak terhubung — bukan fail-open), docker-compose snippet disiapkan sebagai referensi belum diaktifkan. Begitu ClamAV tersedia (di server ini atau terpisah), tinggal isi 2 Setting, tanpa perubahan kode/deploy ulang
- [x] **Backend diimplementasikan & diverifikasi end-to-end** — endpoint baru `POST /api/v1/chat_sessions/:session_id/attachments` (`ChatAttachmentsController`, tanpa `authenticate_and_authorize!`, otorisasi berbasis `session_id` valid). Denylist ekstensi keras + Setting whitelist + batas ukuran (Content-Length & data aktual) + `Service::Chat::VirusScan` (fail-closed) semuanya diuji: file diizinkan (201), executable ditolak (422, denylist), sesi tidak valid (404), file kebesaran (422). Sinkron ke tiket (kalau ada) memakai pola clone `Store` yang sama seperti `CanCloneAttachments`.
    **2 bug ditemukan & diperbaiki selama pengujian**: (1) CSRF — endpoint publik butuh `skip_before_action :verify_csrf_token`, pola yang sama dipakai `FeedbackController` (Fase 1) & native `FormController`; (2) `current_user` selalu nil meski token agent valid dikirim — ternyata `current_user` cuma reader polos, populasinya efek samping memanggil method otentikasi tertentu yang TIDAK otomatis jalan tanpa `authenticate_and_authorize!` — diperbaiki dengan `prepend_before_action :authentication_check_only` (mencoba resolusi user KALAU ada kredensial, tidak memaksa gagal kalau tidak ada — beda dari `authenticate_and_authorize!` yang mewajibkan). Ditemukan juga `stores.created_by_id` NOT NULL (beda dari `chat_messages` yang nullable) — attachment dari customer anonim di-set eksplisit ke user System (id 1) untuk kolom itu, sementara `Chat::Message.created_by_id` tetap nil murni (konsisten dengan alur teks). Semua data uji sudah dibersihkan
- [x] **Frontend PANEL AGENT diimplementasikan** — tombol attachment (`.js-attachButton` + hidden file input), upload via `FormData`/`@ajax` (pola sama `ssl_certificate.coffee`), render bubble attachment dengan link download (`ChatAttachmentsController#show`, endpoint baru), didengarkan lewat broadcast `chat_session_attachment` untuk KEDUA sisi (bukan render optimis terpisah). Lolos precompile
- [x] **Ditemukan & diperbaiki: ikon attachment tidak sejajar tengah dengan kotak input/tombol Send di KEDUA sisi** — root cause NATIF Zammad (parent flex container align ke ujung: `flex-start` di agent, `flex-end` di widget), bukan bug Fase 5 tapi jadi terlihat setelah tombol attach ditambahkan. Diperbaiki `align-self: center` di elemen ikon saja (tidak mengubah parent native). Diverifikasi terukur di kedua sisi — lihat entry 104 `docs/ACTIVITY_LOG_SISKA.md`
- [x] **Frontend WIDGET CUSTOMER diimplementasikan untuk KEDUA varian** — tombol attach + input file di `views/chat.eco`, upload via `FormData` (jQuery `$.ajax` di `chat.coffee` / `XMLHttpRequest` polos di `chat-no-jquery.coffee`), render bubble attachment lewat broadcast `chat_session_attachment` yang sama (bukan render optimis terpisah). URL API diturunkan dari `@options.host` (URL WebSocket) lewat helper baru `apiBaseUrl()` — dikonfirmasi lewat pembacaan `loadCss()` yang sudah melakukan konversi serupa. Build gulp bersih untuk kedua varian, dikonfirmasi kode baru ada di semua 4 artifact hasil build
- [x] **Fitur tambahan: enable/disable attachment global + per-agent** (Section 5.2.6) — atas permintaan user, klarifikasi "default global ON, default agent OFF". Setting baru `chat_attachment_enabled` (global) + preferensi per-agent `preferences[:chat][:attachment_enabled]` (pola sama `preferences[:chat][:active]` yang sudah ada) — DUA-DUANYA harus lolos, dipusatkan di `Chat::Session#attachment_enabled?` (bukan diulang di controller & event handler). Checkbox baru di Chat Settings agent (disembunyikan total kalau Setting global mati). Tombol attach di widget disembunyikan default, ditampilkan sesuai flag baru `attachment_enabled` di payload `chat_session_start` yang sudah ada (server yang memutuskan, widget tidak bisa baca Setting langsung). Diuji semua kombinasi via `rails runner`+HTTP — sesuai spesifikasi. **Ditemukan & diperbaiki saat verifikasi langsung**: checkbox-nya sendiri tidak tampil benar (pola ikon `checkbox`/`checkbox-checked` bermasalah untuk konteks ini) — disederhanakan ke `<input type="checkbox">` native, fungsi/nama field tidak berubah

**Fitur Tambahan — Reply ke Pesan Spesifik (Seperti WhatsApp)**
- [x] **Desain teknis rinci selesai** (Section 5.3, `docs/DESIGN_LIVE_CHAT_ENHANCEMENT.md`) — atas permintaan user, berlaku KEDUA sisi (widget customer & panel agent). Migration baru `chat_messages.reply_to_id` (FK asli, relasi 1-ke-1 jelas — beda dari pola preferences JSON di `chat_sessions`). Backend `ChatSessionMessage` (sudah dibaca detail) diubah terima `reply_to_id` opsional dengan validasi keamanan (pesan yang direferensikan harus dari sesi chat YANG SAMA, mencegah kebocoran potongan pesan lintas-sesi). UI: 3 penyesuaian PARALEL di 2 codebase (widget `public/assets/chat/` + app utama `chat.coffee`) — affordance reply, indikator "membalas...", render kutipan gaya WhatsApp di bubble pesan. Terintegrasi ke sinkronisasi tiket (5.1.4): balasan disalin ke tiket dengan blockquote kutipan asli, pola yang sama dipakai halaman feedback CSAT Fase 1
- [x] **Backend diimplementasikan & diverifikasi end-to-end** — migration `chat_messages.reply_to_id` dijalankan; `ChatSessionMessage` menerima & memvalidasi `reply_to_id` (ditolak diam-diam kalau beda sesi, pesan tetap terkirim tanpa quote); diuji: balasan tersimpan dengan `reply_to_id` benar, disinkron ke tiket sebagai blockquote kutipan asli
- [x] **Frontend PANEL AGENT diimplementasikan** — affordance reply muncul saat hover bubble pesan (ikon `reply`), indikator "membalas..." di atas kotak input dengan tombol batal, render kutipan gaya WhatsApp di bubble pesan baru. Lolos precompile
- [x] **Frontend WIDGET CUSTOMER diimplementasikan untuk KEDUA varian** — affordance reply muncul saat hover bubble pesan (cuma bisa membalas pesan AGENT, bukan pesan sendiri — id server sungguhan cuma diketahui dari pesan yang DITERIMA, keterbatasan yang disengaja diterima, simetris dengan panel agent yang punya keterbatasan sama), indikator "membalas..." di atas kotak input, render kutipan di bubble pesan baru. Build gulp bersih untuk kedua varian

> ⚠️ Kedua item ini menyentuh core code channel chat yang tidak punya extension point resmi — risiko konflik saat upgrade Zammad berikutnya paling tinggi di sini. Dikonfirmasi lewat riset kode langsung, bukan cuma dugaan awal.

## Fase 6 — QA, Merge & Rollout

- [ ] Jalankan test suite Zammad bawaan (`test/`, `spec/`) setelah setiap perubahan
- [ ] Code review & merge tiap feature branch ke `main` satu per satu
- [ ] Rebuild image `zammad-staging-app` dari source terbaru, deploy ke container staging
- [ ] UAT bersama tim PKP di staging sebelum lanjut ke production
- [ ] Rencana migrasi/rollout ke production (mengacu pada `zammad_production_backup.sql.gz` yang sudah ada)

## Di Luar Fase — Anonimisasi Kontak User Non-Admin (Staging)

- [x] **Anonimisasi permanen email/login/phone/mobile untuk 72.017 user non-admin** di staging (`zammad-staging-zammad-app-1`), setelah komplain email testing "nyasar" ke customer asli — ditemukan database staging adalah salinan penuh data production dengan SMTP outbound hidup, bukan data dummy
- [x] Scope: semua user kecuali role Admin, akun sistem (id 1), dan domain `@pkp.co.id` (staf internal PKP + akun service `integration-kpi-api@pkp.co.id`)
- [x] Backup otomatis nilai lama (72.017 baris) tersimpan sebelum eksekusi, dieksekusi lewat SQL mentah (tanpa callback/notifikasi apa pun) — `script/anonymize_non_admin_contacts.rb`
- [x] Dijalankan manual oleh user (eksekusi tulis massal diblokir untuk Claude oleh auto-mode classifier, sesuai pola yang sama dengan koreksi bug timezone sebelumnya)
- [x] Didokumentasikan lengkap (kriteria scope, format transformasi, contoh before/after ter-mask) di `docs/ANONYMIZATION_CONTACT_INFO.md`
- [ ] **Belum diproses**: identitas Telegram (tidak ada kolom per-user yang stabil, kemungkinan cuma ada di `ticket_articles.preferences` per pesan — butuh investigasi terpisah)
- [x] Pindahkan file backup (berisi PII asli) dari `/tmp` container ke penyimpanan permanen di host: `/usr/local/src/claudeai/backups/anonymize_contacts_backup_20260917_010218.jsonl` (checksum SHA-256 diverifikasi cocok sebelum salinan lama di container dihapus)

## Di Luar Fase — Investigasi Notifikasi Email Selama Pengujian

- [x] **Investigasi ditemukan**: 121 notifikasi email (dari 168 total) di 5 tiket yang diperiksa terkirim ke 8 alamat staf/inbox tim asli sebagai efek samping perubahan status tiket saat pengujian (Fase 3 Eskalasi, CSAT) — penyebabnya perilaku bawaan Zammad (notifikasi ke anggota Group saat tiket berubah), bukan bug kode proyek. Metodologi: analisis tabel `histories` (notifikasi jenis ini tidak tercatat di `ticket_articles` maupun `production.log` level INFO)
- [x] **Mitigasi #1 diimplementasikan**: Group baru `QA - Internal Testing` (id 89) dibuat khusus untuk tiket pengujian internal, sengaja **tanpa anggota** (`groups_users` kosong) — diverifikasi `User.group_access(89, 'full')` mengembalikan 0 penerima, sehingga tiket pengujian mendatang di Group ini tidak akan memicu notifikasi ke staf asli. Residual risk (di luar Group ini, tetap dalam kendali): Owner tiket & @mention eksplisit tetap bisa memicu notifikasi — hindari assign owner asli/mention saat testing. Script: `script/create_qa_testing_group.rb`
- [x] **Mitigasi #2 diimplementasikan**: mekanisme toggle SMTP sink untuk dipakai sebelum/sesudah sesi pengujian aktif — container `siska-qa-mailsink` (Mailpit, `script/create_smtp_sink.sh`, idempotent) berjalan di network docker yang sama dengan Zammad; skrip `script/toggle_smtp_sink.rb enable|disable|status` mengalihkan seluruh channel outbound SMTP (id 1, 5, 7) ke sink tersebut dan mengembalikannya. **Siklus penuh (enable → status → disable → status) sudah diuji langsung oleh user di server** — konfigurasi kembali persis ke baseline (`mail.pkp.co.id:465` / `mail.simgroup.co.id:465`), state file bersih setelah disable. Wajib dijalankan `enable` sebelum sesi testing dan `disable` segera setelah selesai — jangan dibiarkan aktif lama karena channel id=1 dipakai operasional notifikasi nyata
- [x] Tiket pengujian yang sudah ada (163027, 163032, 163036 / #10161960, #10161965, #10161969) dipindahkan ke Group `QA - Internal Testing` — lewat `update_columns` (bukan `update!`) supaya perpindahan Group itu sendiri tidak ikut memicu notifikasi ke anggota Group lama maupun baru

## Di Luar Fase — Setting Batas Tiket per Overview (Setting Native Tanpa UI)

- [x] **Ditemukan**: menyusul pertanyaan user apakah Setting ukuran halaman berlaku untuk semua tabel Zammad — dicek satu-satu, ditemukan Overviews (daftar tiket utama yang dipakai semua agent) **sudah punya Setting native sendiri** untuk ini, `ui_ticket_overview_ticket_limit` (default 2000, `app/models/ticket/overviews.rb#limit_per_overview`), tapi **tidak ada tempat di Admin UI mana pun untuk mengubahnya** — cuma bisa lewat Rails console, karena di-seed dengan `options: {}` (tanpa `form:`)
- [x] **Diperbaiki (murni tambah UI, TIDAK ubah logika/nilai)**: `script/expose_ticket_overview_limit_setting.rb` menambahkan `options.form` (satu input angka) ke Setting yang SUDAH ADA, tanpa menyentuh `state`/`preferences`/behavior sama sekali. Ditambahkan tab baru "Overview" di `siska_settings.coffee` (area native `UI::TicketOverview::TicketLimit`). Dikonfirmasi nilai tetap 2000 setelah perubahan, tidak ada perubahan perilaku — cuma sekarang bisa diubah dari Admin > Settings > SISKA > Overview. **Catatan**: permission Setting ini `admin.overview`, lebih sempit dari gerbang tab SISKA (`admin.system`) — admin yang cuma punya `admin.overview` belum bisa akses lewat sini

## Di Luar Fase — Server-Side Pagination pada Sisa Tabel Manage

Menindaklanjuti instruksi eksplisit user: **"buat semua menjadi server-side untuk semua yang masih belum server-side, terlepas datanya besar atau kecil"**. Detail lengkap survei & keputusan cakupan di `docs/DESIGN_REPORTING_FRT.md` Section 8.

- [x] **Ticket States** (`ticket_state.coffee`) diubah dari client-side ke server-side (`pagerAjax: true` + `show(params)` override, pola sama persis `group.coffee` native) — tidak ada perubahan backend, `App.ControllerGenericIndex` native sudah menerima `page`/`per_page`
- [x] **Ticket Priorities** (`ticket_priority.coffee`) — perlakuan identik dengan Ticket States
- [x] **Checklist Templates** (`checklist_template.coffee`) — perlakuan identik, dicatat juga bahwa `render()`-nya sendiri rebuild `@genericController` saat Setting berubah (hal terpisah dari pagination berbasis route)
- [x] **Time Accounting Types — DIPUTUSKAN FINAL TETAP DILEWATI**, setelah diriset lebih dalam atas permintaan user. Ditelusuri sampai ke level klik pager sungguhan (`App.ControllerTable#paginate`) — kalau `pagerAjax` diaktifkan di sini, klik nomor halaman akan `@navigate` ke route yang TIDAK terhubung balik ke state tab ini (karena ini TAB di dalam `#manage/time_accounting`, bukan route sendiri, dan `show()` bawaan `ControllerTabs` tidak meneruskan param apa pun ke tab), sehingga secara FUNGSIONAL rusak (bukan cuma beda gaya kode) — klik halaman 2 tidak benar-benar mengambil data halaman 2. Genuinely mengonversi butuh salah satu dari dua hal yang tidak sepadan manfaatnya: menulis ulang seluruh CRUD tab ini dari nol, atau mengubah kode inti `ControllerTable`/`ControllerGenericIndex` yang dipakai bersama puluhan halaman Admin lain. User setuju dilewati. Detail lengkap di `docs/DESIGN_REPORTING_FRT.md` Section 8
- [x] **Manage > AUX Status** diubah ke server-side sungguhan + bug pencarian luas ditemukan & diperbaiki — lihat entri terkait di Fase 4 di atas dan `docs/DESIGN_AUX_STATUS.md` Section 6s
- [x] **Tampilan tabel Manage > AUX Status disamakan dengan tabel native Zammad lain** — markup header (`table-column-head`/`table-column-title`), pager di atas & bawah tabel, empty-state (`table--placeholder`) diselaraskan persis dengan `views/generic/table.jst.eco`/`generic/admin/empty.jst.eco`. Detail di `docs/DESIGN_AUX_STATUS.md` Section 6t
- [x] **Ticket Overviews — DIPUTUSKAN TIDAK DIKONVERSI**, setelah dikomunikasikan risikonya ke user secara eksplisit. Ditemukan bahkan implementasi Zammad generasi terbaru (GraphQL, `Gql::Queries::Tickets::Cached::ByOverview`) SENGAJA tidak memakai `LIMIT`/`OFFSET` per-halaman untuk Overviews — mengambil semua tiket sampai batas sekaligus, karena urutan tiket (`updated_at`/prioritas) sering berubah antar-poll, OFFSET-based pagination berisiko bikin tiket duplikat/hilang antar-halaman. Katup pengaman resmi untuk performa tetap Setting `ui_ticket_overview_ticket_limit` (sudah diberi UI, lihat bagian "Di Luar Fase" di bawah). User mengonfirmasi: "tidak perlu dirubah". Detail di `docs/DESIGN_REPORTING_FRT.md` Section 8
- [x] **Setting bersama untuk ukuran halaman SEMUA 17 tabel Admin > Manage yang server-side**, atas pertanyaan user "apakah ada tabel yang limit perpage nya masih belum configurable?" — disurvei, ditemukan 17 halaman (14 native Zammad + 3 yang baru dikonversi) semuanya hardcode `pagerPerPage: 50`, tidak satu pun Setting-driven (konvensi native Zammad, bukan terlewat khusus proyek ini). Atas permintaan "tambahkan untuk semuanya", dibuat SATU Setting bersama `ui_admin_list_per_page` (default 50, `frontend: true`, tab baru "Admin List" di Admin > Settings > SISKA) — bukan 17 Setting terpisah, karena ke-17 halaman ini konseptual sama (ukuran halaman CRUD Admin generik). Ke-17 file diubah `pagerPerPage: 50` → baca Setting dengan fallback 50 (perilaku tidak berubah sampai admin ubah nilainya). **Catatan**: 14 dari 17 file adalah kode ASLI Zammad, blast radius lebih luas dari biasanya — perubahannya sendiri mekanis/rendah risiko (ganti angka tetap dengan baca Setting yang default-nya sama). Detail di `docs/DESIGN_REPORTING_FRT.md` Section 8
- [ ] Verifikasi interaktif di browser (klik nomor halaman sungguhan, ubah `ui_admin_list_per_page` dari Admin UI dan lihat efeknya) untuk Ticket States/Priorities/Checklist Templates + 14 halaman native lain — baru dikonfirmasi lewat `grep` compiled bundle & `rails runner`, belum diklik langsung oleh user

## Fase 6 — Live Chat untuk Semua User Login

Atas permintaan user: "saya mau semua user punya fitur chat, untuk user yang login, gunakan jendela chat nya sama seperti jendela chat agent, hanya tidak ada fitur pengaturan attachment seperti agent, dan tidak bisa enable disable chat". Detail lengkap di `docs/DESIGN_CHAT_SELF_SERVICE.md`.

- [x] **Riset selesai** — dikonfirmasi backend live chat (`chat_session_init`/`chat_status_customer`/`chat_session_message`) TIDAK punya pengecekan permission untuk "siapa boleh jadi customer", status agent/customer ditentukan otomatis dari `created_by_id == chat_session.user_id` (bukan dari permission pemanggil) — jadi user login mana pun otomatis berperan sebagai customer, tidak mungkin tertukar jadi agent. Ditemukan juga: checkbox pengaturan attachment & toggle enable/disable topik chat 100% hidup di `App.CustomerChat` (controller halaman `#customer_chat`), BUKAN bagian `ChatWindow` (komponen jendela percakapan itu sendiri) — jadi requirement "tanpa 2 fitur itu" otomatis terpenuhi kalau `ChatWindow` dipakai ulang TANPA membungkusnya dalam `App.CustomerChat`, tanpa perlu logika exclude tambahan. Pengaman kapasitas (`max_queue: 10`, 8 agent aktif) sudah native, dipakai ulang bukan dibuat baru
- [x] **Keputusan user**: rollout ke semua user login sekaligus (bukan role tertentu dulu untuk uji coba); UI menunggu agent pakai pola modal-di-atas-kerangka-jendela-chat (meniru mekanisme `.zammad-chat-modal` widget yang sudah teruji), bukan halaman penuh terpisah
- [x] **Desain teknis rinci selesai (REVISI PERTAMA)** (`docs/DESIGN_CHAT_SELF_SERVICE.md`) — `ChatWindow` diekspor jadi `App.ChatWindow` (rename murni, tanpa ubah logika) supaya dipakai ulang oleh controller baru `App.MyChat`; route+menu baru `#my_chat` dengan `permission: ['*']` (pola native Zammad yang sama dipakai `Dashboard`, bukan hack); identitas chat (name/email) untuk user login diambil OTOMATIS dari akun asli (bukan form manual, DIABAIKAN dari payload client) — sekaligus jadi pengaman keamanan (tidak bisa menyamar) dan manfaat tambahan (tiket hasil chat tertaut ke akun SISKA asli, bukan bikin user anonim baru); Setting kill-switch baru `chat_self_service_enabled` (default `false` sampai pengujian selesai) dicek HANYA di jalur user login (`@session['id']` terisi), sengaja dipisahkan eksplisit di kode supaya kalau dimatikan TIDAK ikut mematikan widget publik yang dipakai visitor anonim asli; Setting `chat_self_service_chat_id` untuk topik default (tidak di-hardcode di JS). Tidak ada migration baru — 100% pakai ulang `Chat::Session`/`Chat::Message` yang sudah ada
- [x] **Requirement DIREVISI user: chat cuma untuk FOLLOW-UP tiket yang sudah ada, bukan cara baru bikin tiket** — "chat akan muncul kalau sudah membuat ticket ... chat tidak membuat ticket, tapi chat dikaitkan dengan ticket yang sudah ada". Desain diperbarui total (REVISI KEDUA, `docs/DESIGN_CHAT_SELF_SERVICE.md`): menu "Live Chat" menampilkan DAFTAR tiket milik user sendiri (bukan tombol "Mulai Chat" bebas) — dipilih user lewat AskUserQuestion drpd tombol di halaman detail tiket, supaya tidak menyentuh kode `ticket_zoom` yang kompleks & dipakai bersama agent. Cuma tiket berstatus TERBUKA (`state.state_type.name` bukan `closed`/`merged`) yang muncul & bisa di-follow-up (keputusan user). `ticket_id` sekarang WAJIB & divalidasi (harus milik user login + masih terbuka) di `ChatSessionInit#run` saat `chat_session_init`, BUKAN diisi belakangan lewat auto-create seperti Item 5 -- `create_ticket_for_chat_session` (`ChatSessionStart`) cukup ditambah guard `return if chat_session.ticket_id.present?` di baris teratas (satu-satunya perubahan di file itu), jalur widget anonim (Item 5 asli) sama sekali tidak terpengaruh. `sync_message_to_ticket` TIDAK PERLU diubah sama sekali -- sudah generic sejak Fase 5, otomatis mengalirkan tiap pesan jadi artikel di tiket yang dipilih, terlepas kapan `ticket_id` terisi. Routing SENGAJA tetap ke topik "Customer Service" tunggal (keputusan user) -- TIDAK dirancang routing per-departemen pemilik tiket (perubahan besar terpisah kalau nanti dibutuhkan)
- [x] **Requirement tambahan: riwayat chat sebelumnya (Fase 5, 5.1.7) ditampilkan di KEDUA sisi — user & agent** — beda dari Fase 5 yang sengaja cuma agent (waktu itu "customer" masih visitor anonim, sekarang user login dengan akun asli). Ditemukan sekaligus DIPERBAIKI celah desain: payload `chat_session_start` ke sisi customer TERNYATA berbentuk beda (field datar ala widget lama) dari yang diterima agent (`{session: session_attributes}` lengkap) — `App.ChatWindow` butuh bentuk yang SAMA seperti agent di KEDUA sisi supaya bisa jalan sama sekali, bukan cuma soal riwayat. Diperbaiki lewat cabang baru di `ChatSessionStart#run` berdasarkan penanda `preferences[:self_service]` (diset saat `chat_session_init`) — jalur widget anonim (Item 5 asli) sama sekali TIDAK BERUBAH. Begitu payload disamakan, riwayat+transkrip+affordance panah OTOMATIS muncul di kedua sisi TANPA perubahan apa pun lagi di template/CSS (sudah generic sejak Fase 5). Lihat `docs/DESIGN_CHAT_SELF_SERVICE.md` Section 5.5
- [x] **Requirement tambahan: routing dibuat configurable** — "untuk routing, saya mau dibuat configurable di setting, bisa diarahkan ke group terkait, tapi default ke customer service". Ditemukan sumber kebenaran yang SUDAH ADA (tidak perlu Setting mapping baru): field `chat.preferences[:ticket_group_id]` (Fase 5, sebelumnya cuma dipakai arah "chat topik ini → buat tiket di Group X") dipakai ULANG arah sebaliknya — cari topik `Chat` yang `ticket_group_id`-nya cocok dengan Group tiket yang di-follow-up, fallback ke Setting default (`chat_self_service_chat_id`) kalau tidak ketemu ATAU topiknya ada tapi tidak ada agent aktif (supaya user tidak macet). Setting baru `chat_self_service_route_by_ticket_group` (default `true`) sebagai kill-switch kalau admin mau matikan pencarian ini sama sekali. Karena instance ini baru punya SATU topik chat, hasilnya hari ini otomatis tetap selalu "Customer Service" — disiapkan plug-and-play untuk nanti (pola sama ClamAV Fase 5), TANPA perubahan perilaku langsung. Lihat `docs/DESIGN_CHAT_SELF_SERVICE.md` Section 5.6
- [x] **Implementasi kode selesai** — `App.ChatWindow` (rename ekspor), controller baru `App.MyChat` (`app/assets/javascripts/app/controllers/my_chat.coffee`) + 2 view baru (`my_chat/ticket_list.jst.eco`, `my_chat/waiting.jst.eco`) + CSS baru (`my_chat.scss`); backend `ChatSessionInit#self_service_init` (validasi ticket_id, identitas dari akun, routing per-Group), `ChatSessionStart` (guard skip auto-create, cabang payload self_service); 3 Setting baru (`chat_self_service_enabled` default `false`, `chat_self_service_chat_id`, `chat_self_service_route_by_ticket_group` default `true`) via `script/create_chat_self_service_settings.rb`
- [x] **Diverifikasi end-to-end lewat Playwright (bukan Puppeteer, sesuai keputusan baru user)** — alur penuh: TAD tester lihat tiketnya sendiri di daftar → klik Chat → antrean → agent accept → `App.ChatWindow` identik muncul di KEDUA sisi → kirim pesan dua arah → dikonfirmasi TIDAK ADA tiket baru terbuat, pesan tersinkron sebagai artikel di tiket yang dipilih. Validasi keamanan dites lewat browser sungguhan juga: tiket milik user lain → ditolak; tiket closed milik sendiri → ditolak; tiket terbuka milik sendiri → lolos
- [x] **4 bug nyata ditemukan & diperbaiki selama pengujian** (bukan cuma desain di atas kertas) — `App.Chat.find()` undefined di sisi user (crash `.displayName()`, diperbaiki `?.`), `chat.render()` ternyata tidak otomatis dipanggil constructor (jendela kosong tanpa ini, ditelusuri dari pola `addChat` yang sudah ada), field query pencarian tiket salah (`customer.id:` → `customer_id:`, dikonfirmasi dari kode native `ticket/search.rb`), filter state tiket di frontend perlu dibuat defensive (race `App.TicketState` belum siap). Detail lengkap di `docs/DESIGN_CHAT_SELF_SERVICE.md` Section 7.1
- [x] **Ditemukan & diperbaiki masalah INFRASTRUKTUR di luar kode Fase 6**: disk server 96% penuh memicu Elasticsearch memblokir index baru ("flood-stage watermark") — tiket baru gagal ter-index/tidak muncul di pencarian manapun (berpotensi memengaruhi fitur lain juga, bukan cuma Fase 6). Dibersihkan Docker build cache (~6,9GB, disetujui user) + index block dibuka manual lewat Elasticsearch API (ES tidak otomatis pulih sendiri walau disk sudah longgar). **Residual**: tiket lain yang mungkin gagal ter-index selama periode ini belum di-reindex ulang menyeluruh — di luar cakupan Fase 6
- [x] **Ditemukan & diperbaiki (laporan user setelah coba sendiri): tombol attachment tidak muncul di jendela follow-up milik user** — root cause: `ChatWindow#render()` menentukan tampil/tidak tombol attach dari preferensi PRIBADI siapa pun yang login (benar untuk agent, salah total untuk user login Fase 6 yang bukan agent — preferensi mereka tidak pernah ada artinya). Diperbaiki: dibedakan lewat `@Session.get('id') is @session.user_id` (pola yang sama dipakai backend membedakan agent/customer) — agent tetap pakai preferensi pribadi, user login pakai `@session.attachment_enabled` (field baru ditambahkan ke `session_attributes`). Diverifikasi lewat Playwright: tombol muncul di sisi user, TIDAK ADA regresi di sisi agent. Detail di `docs/DESIGN_CHAT_SELF_SERVICE.md` Section 7.1 poin 7
- [x] **Ditemukan & diperbaiki (laporan user): jendela chat aktif tidak selebar/sepenuh sisi agent** — root cause: `.chat-window` butuh parent `display:flex` (`.chat-workspace`, native) supaya sizing bawaannya bisa membesar, sebelumnya ditempel langsung ke elemen dibatasi `max-width:640px`. Diperbaiki: batas lebar dipindah ke class baru `.my-chat-content` (khusus daftar tiket & menunggu), `renderActive` membungkus jendela dengan `.chat-workspace` (class native, bukan CSS baru). Diverifikasi terukur lewat Playwright: lebar jendela ~98% lebar halaman. Detail di `docs/DESIGN_CHAT_SELF_SERVICE.md` Section 7.1 poin 8
- [x] **Riwayat chat sebelumnya (poin 7 requirement) DIKONFIRMASI muncul di kedua sisi** lewat pengujian 2-putaran (chat → disconnect+close → chat kedua untuk tiket sama) via Playwright — meta panel kedua sisi menampilkan "Previous chats from this visitor" lengkap. Detail di `docs/DESIGN_CHAT_SELF_SERVICE.md` Section 7.1 poin 9
- [x] **Atas permintaan user: tampilan "menunggu agent" diubah jadi gaya loading native full-screen, teks tidak berubah** — kartu kecil custom diganti class native `.fullscreenMessage.fullscreenMessage--placeholder` + `.loading.icon` (dipakai ulang dari `layout_ref/loading_placeholder.jst.eco`, bukan CSS baru). Diverifikasi lewat Playwright: elemen mengisi 1112x800px, teks sama persis. Detail di `docs/DESIGN_CHAT_SELF_SERVICE.md` Section 7.1 poin 10
- [x] `chat_self_service_enabled` DIAKTIFKAN sementara (atas persetujuan eksplisit user) supaya user bisa coba sendiri — statusnya SAAT INI **aktif** di server (bukan `false`), belum berlaku "resmi" untuk semua 72.040 user (belum ada pengumuman/rollout formal, cuma dipakai untuk pengujian mandiri user)
- [ ] Verifikasi kill-switch belum dites eksplisit lewat browser (widget publik tetap jalan normal saat `chat_self_service_enabled` dimatikan) — sudah dipastikan terpisah secara kode, belum dibuktikan lewat pengujian langsung
- [ ] Keputusan final rollout resmi ke SEMUA 72.040 user (kapan, ada pengumuman internal atau tidak) — belum diputuskan eksplisit, Setting saat ini aktif hanya karena permintaan uji coba

## Fase 7 — Widget Live Chat Bergaya Tab (Home / Messages / Help)

Atas permintaan user: "saya mau, widget chat visitor dibuat meniru gaya gaya claude, ada home, message, help. help menggunakan data dari knowledge base, kamu riset kebutuhannya". Detail lengkap di `docs/DESIGN_WIDGET_HOME_MESSAGES_HELP.md`.

- [x] **Riset selesai**: Zammad SUDAH punya Knowledge Base publik (`POST /api/v1/knowledge_bases/search`, anonim, otomatis dibatasi ke artikel `published` saja — tidak perlu logika visibilitas baru). TAPI ditemukan endpoint itu TIDAK mengirim header CORS untuk request anonim — dikonfirmasi ke user (AskUserQuestion) bahwa widget ini AKAN ditempel di domain lain (bukan satu domain dengan server SISKA), jadi CORS jadi masalah NYATA, bukan teoretis. Solusi dipilih: proxy pencarian KB lewat koneksi WebSocket yang widget SUDAH SELALU punya (bukan REST call baru lintas-domain) — menghindari perlu melonggarkan CORS controller KB untuk SEMUA origin
- [x] **Ditemukan sekaligus (di luar scope Help tab, tapi bug nyata)**: fitur attachment upload (Fase 5, SUDAH LIVE) punya celah CORS YANG SAMA — belum pernah diuji di domain yang genuinely berbeda (semua pengujian sejauh ini kebetulan same-origin). Disepakati (AskUserQuestion) diperbaiki BERSAMAAN — tambah header CORS eksplisit ala `FormController` (satu-satunya preseden native "publik+anonim+lintas-domain" di codebase ini) KHUSUS `ChatAttachmentsController`
- [x] **Riset struktur widget**: dikonfirmasi TIDAK ADA state machine terpusat (tiap event WebSocket independen menimpa 2 slot tetap) — bagus untuk rencana ini, konten Messages yang sudah ada bisa DIBUNGKUS APA ADANYA tanpa mengubah selector/logic manapun. TIDAK ADA sistem ikon sprite (semua SVG inline) dan TIDAK ADA badge unread angka (cuma highlight CSS per-bubble) — keduanya bukan kerja baru yang diminta, dicatat sebagai referensi kalau nanti dibutuhkan
- [x] **Keputusan user**: tab Home berisi sapaan + 2 tombol pintasan ("Kirim Pesan"/"Cari Bantuan"), BUKAN versi dengan daftar artikel populer/promoted (opsi yang lebih kompleks, butuh konsep "artikel promoted" baru — tidak dipilih)
- [x] **Desain teknis rinci selesai** (`docs/DESIGN_WIDGET_HOME_MESSAGES_HELP.md`) — 4 view baru (`tabbar`, `home`, `help`, `kb_result`), restrukturisasi MINIMAL `chat.eco` (bungkus konten Messages yang sudah ada dalam 1 div baru, bukan rombak), event WebSocket baru `chat_knowledge_base_search` (file baru `lib/sessions/event/chat_knowledge_base_search.rb`, proxy ke `SearchKnowledgeBaseBackend` yang sudah ada), perbaikan CORS `ChatAttachmentsController`. Dikerjakan di KEDUA varian widget (jQuery + non-jQuery, pola risiko yang sudah berulang sejak Fase 5)
- [x] **Backend**: `ChatAttachmentsController` diberi `after_action :set_access_control_headers_execute` (mirror `FormController`) — `before_action :cors_preflight_check` TERNYATA sudah otomatis ter-include dari `ApplicationController::SetsHeaders`, jadi cuma SATU baris tambahan yang benar-benar dibutuhkan. Diverifikasi lewat curl dengan header `Origin` palsu: header CORS sekarang muncul di response GET/POST (200/403/422), tidak ada regresi di alur upload yang sudah ada
- [x] **Backend**: `lib/sessions/event/chat_knowledge_base_search.rb` (event WebSocket baru) — proxy ke `SearchKnowledgeBaseBackend` (`user: nil`, otomatis dibatasi ke artikel `published`), URL absolut lintas-domain-aman dibangun manual dari `Setting.get('http_type')`/`fqdn` (bukan `custom_path_if_needed` yang butuh `request` object, tidak tersedia di event WebSocket). Diverifikasi lewat `Sessions::Event.run` langsung: query kosong & tidak ada hasil sama-sama balik array kosong tanpa error; query cocok balik hasil dengan highlight & URL yang benar-benar resolve (curl 200)
- [x] **Frontend (KEDUA varian, jQuery & non-jQuery)**: 4 view baru (`tabbar.eco`, `home.eco`, `help.eco`, `kb_result.eco`), `chat.eco` dibungkus jadi 3 `.zammad-chat-tab-body` (home/messages/help, messages = konten lama APA ADANYA) + `.zammad-chat-tabbar` baru. Method baru `switchTab`, `onKbSearchInput` (debounce 400ms), `onKnowledgeBaseSearchResult`. Tab default: **Home**; kalau ada sesi berjalan (reconnect, `@sessionId` sudah terisi saat `open()`) langsung ke **Messages**. Perhitungan animasi buka/tutup (`remainerHeight`) disesuaikan ikut mengurangi tinggi tab bar. CSS baru di akhir `chat.scss` (tab bar, Home, Help, highlight hasil KB)
- [x] **Build**: `pnpm install` + `npx gulp build` bersih untuk kedua varian (`chat.js`/`.min.js`, `chat-no-jquery.js`/`.min.js`, `chat.css`) — dikonfirmasi lewat `node --check` (syntax valid) & grep ke bundle hasil compile. Di-deploy ke `zammad-staging-zammad-app-1` (widget disajikan sebagai static file, tidak perlu asset pipeline Rails)
- [x] **Deploy event WebSocket baru**: `chat_knowledge_base_search.rb` di-`docker cp` ke KEDUA container SEJAK riset awal, TAPI baru ketahuan saat pengujian browser sungguhan bahwa container `zammad-websocket` perlu **direstart** supaya constant baru ke-load (proses Ruby long-running, bukan auto-reload seperti dev server) — dicatat sebagai pengingat disiplin deploy 2-container yang sudah ada sejak Fase 5
- [x] **Pengujian genuinely lintas-domain (Playwright)**: domain palsu `http://client-website-fake.test:8199` (server HTTP lokal, host di-map lewat Chrome `--host-resolver-rules`) memuat widget LANGSUNG dari `https://helpdesk.satu.solutions` sungguhan (bukan trik same-origin seperti kesalahan pengujian Fase 5). Terkonfirmasi: (1) tab Home tampil default, switch ke Messages/Help lewat tombol/tab bar berfungsi; (2) tab Help — cari "reset password" → hasil KB muncul dengan highlight & link absolut → klik → tab baru terbuka ke halaman artikel asli dan termuat; (3) tab Messages — prechat → antre → agent accept → tukar pesan → **upload attachment BENAR-BENAR lintas-domain berhasil** (sebelumnya diblokir browser tanpa fix CORS) — dikonfirmasi lewat screenshot & log WebSocket (`chat_session_attachment` diterima dengan `store_id` asli), zero console/JS error di sisi customer sepanjang pengujian
- [ ] Keputusan final: 30 artikel Knowledge Base yang ADA SAAT INI semuanya berstatus **belum publish** (internal-only) — tab Help visitor akan kosong sampai tim konten mem-publish artikel (satu artikel test dibuat untuk verifikasi, id 76, judul "Test Fase 7 - Cara Reset Password", TIDAK dihapus karena berguna sebagai fixture pengujian lanjutan — bisa dihapus/diubah publish-nya kapan saja tanpa memengaruhi kode)
- [x] **Revisi struktur (atas permintaan user + 2 screenshot referensi)**: widget diubah dari "pil header + tab bar mengambang, selalu terlihat sejak halaman dimuat" jadi **"tombol bulat mengambang saja saat tertutup, panel Home/Messages/Help TERPISAH muncul saat diklik"** (meniru gaya Intercom/Claude). View baru `launcher.eco`; elemen panel (`@el`) TIDAK diubah maknanya di kode (tetap `.zammad-chat`, semua `@el.find(...)`/`@el.querySelector(...)` yang sudah ada tidak disentuh) — cukup ditambah referensi baru `@launcherEl`. Animasi buka/tutup LAMA (hitung tinggi panel + geser posisi manual) DIHAPUS TOTAL, diganti transisi CSS murni (opacity+transform) dipicu 1 class, disatukan polanya di KEDUA varian (jQuery & non-jQuery)
- [x] **Bug ditemukan & diperbaiki saat verifikasi sendiri**: tombol silang penutup di header sempat TIDAK BISA diklik (elemen pembungkusnya collapse jadi 0x0 karena kombinasi `float`+anak `position:absolute`) — dipindah ke elemen anak yang benar-benar berukuran
- [x] **Diverifikasi Playwright genuinely lintas-domain**: tertutup = cuma tombol bulat (opacity 0, pointer-events none, dicek programatik bukan cuma dilihat); klik = panel muncul (opacity 1, ikon tombol jadi silang, tab default Home); tutup lewat tombol ATAU silang header sama-sama berhasil; regresi PENUH Fase 7 (KB search + upload attachment lintas-domain) diulang dan lolos tanpa error; tampilan mobile (390x800) juga dicek — fullscreen saat terbuka, tombol tetap terlihat sebagai penutup
- [x] **Revisi tampilan (atas permintaan user, 4 screenshot referensi widget Intercom asli)**: header 2-mode (gelap+sapaan besar utk Home, putih+judul tengah utk Messages/Help, ATAU info agent asli saat sedang chat), ikon tab bar outline (nonaktif)/terisi-biru (aktif) sebagai SVG terpisah bukan cuma ganti warna, kotak pencarian bergaya pil+ikon kaca pembesar, baris hasil KB dgn chevron, status "No messages" (ikon+judul+subteks+tombol CTA) SEBELUM form pra-chat muncul. **Sengaja TIDAK direplikasi** (dicatat eksplisit ke user): navigasi "collections" KB (tetap search-only, 30 artikel yang ada belum ada yang publish) & persona bot "Fin AI Agent"+tombol quick-reply (SISKA pakai agent manusia sungguhan, bukan bot — meniru itu akan menyesatkan)
- [x] **3 bug ditemukan & diperbaiki sendiri saat proses ini**: (1) `onWebSocketClose` masih menyentuh `@el` bukan `@launcherEl` (sisa revisi tombol mengambang sebelumnya); (2) `.zammad-chat-modal` (status kosong/tab Messages) `position:absolute` tanpa leluhur ber-`position` di tab-body-nya sendiri — menutupi & memblokir klik tab bar setiap kali tab Messages aktif & modal terisi (sekarang SELALU terisi sejak status kosong ditambahkan); (3) ikon SVG tab bar baru tanpa `width`/`height` eksplisit — render kegedean, bikin label "Messages" turun baris terpisah
- [x] **Diverifikasi Playwright genuinely lintas-domain**: tampilan tiap tab dibandingkan visual ke referensi (sangat mendekati); regresi PENUH (KB search, kirim pesan, upload attachment lintas-domain) diulang & lolos tanpa error; tampilan mobile dicek ulang
- [x] **Eksekusi widget CUSTOMER ke kode sungguhan** (atas instruksi "eksekusi widget chat dulu" — sisi agent/settings/riwayat/user-login MENYUSUL): warna/radius/font Able Pro (`$siska-primary #4680FF` dkk., diambil dari `vuetify.ts`), header tint biru (bukan gelap), logo placeholder di Home, tab bar disederhanakan (1 ikon/tombol, bukan outline+filled), alur Messages disederhanakan (langsung ke form pra-chat, status "belum ada percakapan" dihapus), form pra-chat & layar menunggu direstyle total (spinner cincin baru), tab Help (ikon kiri, ikon-avatar hasil, cuplikan 1 baris), bubble pesan agent jadi putih+border. **Fitur baru: emoji picker** (panel grid 18 emoji, sisip ke kursor, KEDUA varian widget)
- [x] **2 bug ditemukan & diperbaiki lewat Playwright**: badge posisi antrean kehilangan spasi ("position1.") akibat kuirk `display: inline-flex` menghilangkan whitespace antar node teks — diganti `inline-block`; emoji tampil kotak "tofu" di server pengujian (dikonfirmasi via `fc-list`: server ini memang tidak punya font emoji sama sekali, bukan bug) — tetap ditambahkan font emoji eksplisit ke font-family untuk kompatibilitas defensif
- [x] **Diverifikasi Playwright genuinely lintas-domain**: alur penuh Home→Help→Home→form pra-chat langsung→layar menunggu (dikonfirmasi tampil via `initialQueueDelay` 10 detik)→agent accept→tersambung→emoji picker end-to-end→kirim pesan→upload attachment lintas-domain, zero console error
- [x] **Atas keluhan user ("mockup sama hasil berbeda", minta 100% identik, tab Home dulu): audit pixel-accuracy rinci selesai untuk tab Home**, dibandingkan langsung ke source `Home.dc.html`/`Closed.dc.html`. **5 bug nyata ditemukan & diperbaiki**: (1) ukuran panel masih warisan native lama `33em x 30em` (396x360px), tidak pernah diikutkan berubah sepanjang Fase 7/8 padahal SEMUA mockup di 380x640px — diganti `380px x 640px` langsung (px, bukan `em`); (2) `box-sizing` tidak disamakan ke mockup (`border-box`), bikin ukuran aktual meleset 2px — ditambahkan; (3) `.zammad-chat-header-controls` masih pola native `position:absolute` strip-ikon-penuh-tinggi, beda struktur total dari mockup (flex-row + tombol bulat kecil biasa) — `chat.eco`+CSS direstrukturisasi; (4) ikon tombol mengambang (`launcher.eco`, state terbuka MAUPUN tertutup) tidak sama sama sekali bentuknya dgn mockup — diganti persis; (5) **`views/tabbar.eco` ternyata punya wrapper `<div class="zammad-chat-tabbar">` DOBEL** bersarang dgn placeholder yang sudah ada di `chat.eco` — bikin 3 tombol tab berbagi lebar sempit (~103px) bukan lebar panel penuh (378px), label jadi mepet tanpa jarak ("HomeMessagesHelp"). Kemungkinan bug LAMA sejak Fase 7 (entri 115), baru kentara sekarang setelah diukur px-per-px (bukan cuma dilihat sekilas)
- [x] **Ditemukan sekaligus (disiplin build, bukan bug fitur)**: `views/*.eco` widget DIKOMPILASI ke bundle `chat.js`/`chat-no-jquery.js` saat build time (`gulp-eco`) — BUKAN dibaca dari disk saat runtime. Ubah SATU file `.eco` widget WAJIB `npx gulp build` ulang dulu (dari `public/assets/chat/`, bukan root repo) sebelum deploy JS bundle, kalau tidak perubahan tidak akan muncul sama sekali walau file `.eco` di container sudah benar
- [x] **Diverifikasi Playwright genuinely lintas-domain** (skrip baru `verify_home_pixel.js`, fokus baca `getBoundingClientRect`/`getComputedStyle` per elemen Home, bukan cuma screenshot sekilas) — SEMUA nilai terukur (ukuran panel, posisi launcher, header, tombol silang, logo, 2 tombol aksi, tab bar 3-kolom sama lebar) cocok 100% ke angka source mockup setelah kelima bug di atas diperbaiki. Sisa perbedaan yang TIDAK bisa diperbaiki dari sisi widget (sudah didokumentasikan sejak Fase 8): emoji tofu-box (server pengujian tidak punya font emoji) & font Inter tidak dimuat dari Google Fonts (widget sengaja tidak memuat font eksternal, cross-origin)
- [x] **Atas laporan lanjutan user ("ukuran dan posisi button belum proporsional, gunakan struktur ablepro, jangan pakai struktur zammad, kerjakan kembali hanya home")**: 2 tombol Home ternyata masih mewarisi class `.zammad-chat-button` (native Zammad lama, ada `margin` `em` tersembunyi yang tidak pernah di-override — akar masalah "posisi") + ukuran (padding/font/font-weight) bukan angka Able Pro sungguhan (dicek langsung ke starter kit: `_VButtons.scss` global `text-transform:capitalize; font-weight:400`, preseden CTA nyata `AuthForgotPwd.vue` pakai `size="large" rounded="md" block variant="flat"` = height 44px/font 16px/padding-inline 24px). Class lama dilepas total dari `home.eco`, `.zammad-chat-home-action` dibuat berdiri sendiri sesuai struktur Able Pro asli; `.zammad-chat-home` ditambah `justify-content:center` supaya hero content tidak lagi mepet ke atas dgn ruang kosong besar di bawah
- [x] Diverifikasi Playwright: tombol terukur persis height 44px/padding 0 24px/font 16px/400, teks tampil kapital otomatis via CSS (bukan hardcode), konten hero tampil ditengahkan vertikal
- [x] **Atas permintaan user ("gunakan struktur ablepro untuk semua element dalam widget, jangan menggunakan struktur zammad"): restrukturisasi MENYELURUH seluruh widget customer**, dicek ke 4 file starter kit BARU (`ChatSendMsg.vue`, `ChatDetail.vue`, `ChatListing.vue`, `_VButtons.scss`) + token warna baru dari `vuetify.ts` (`error`, `lightsecondary`/containerBg, `lightwarning`). Ringkasan: (1) `.zammad-chat-button` (base native lama) DIHAPUS TOTAL dari semua view, tombol besar konsisten 1 struktur; (2) send/attach/emoji jadi ICON BUTTON sungguhan (send = ikon SAJA warna primary, dulu pil teks); (3) area pesan latar abu-abu `containerBg`, bubble TANPA border (dulu ada border sbg kompensasi latar putih yg salah); (4) field teks (pra-chat, cari KB) dapat STATE FOKUS sungguhan (dulu tidak ada sama sekali); (5) semua warna literal (`hsl()`, `#d32f2f`, `#fff3cd`, `#f8f9fa` berulang) diganti token Able Pro asli; (6) sisa unit `em` warisan native dikonversi px, menuntaskan pola dari entri sebelumnya
- [x] **1 bug pra-existing (BUKAN dari perubahan hari ini) ditemukan lewat screenshot review**: pita abu-abu kosong di atas form pra-chat/status — `.zammad-chat-modal { top: 3.5em }` sudah SEJAK Fase 7 (entri 117) menghitung dobel offset header stlh `.zammad-chat-tab-body` diberi `position:relative` utk bug lain. Diperbaiki `top: 0`
- [x] Diverifikasi Playwright genuinely lintas-domain, alur PENUH (Home→Help→pra-chat dgn state fokus→waiting→connected→emoji→kirim pesan), zero console error
- [x] **Koreksi user (screenshot widget sungguhan): `loader.eco` (layar "Connecting" 3-titik yang tampil PERSIS setelah submit form pra-chat, sebelum `waiting.eco` muncul 10 detik kemudian) TERLEWAT dari semua audit sebelumnya** — tertutup terlalu cepat di setiap pengujian Playwright saya sendiri (yang selalu sengaja menunggu penuh 10 detik). Dirombak jadi PERSIS struktur spinner `waiting.eco` (transisi mulus, cuma badge posisi yg menyusul). Diverifikasi Playwright dgn screenshot SEGERA (1.2 detik) setelah submit — persis skenario user
- [x] **Konfirmasi user: "Batalkan" = keluar dari antrian kembali ke home.** Diimplementasikan: tombol "Cancel" di `waiting.eco` DAN `loader.eco` (tersedia sejak awal antre, tidak perlu tunggu 10 detik). Backend TIDAK perlu event baru — `chat_session_close` yang sudah ada terbukti valid dipanggil sebelum tersambung ke agent (dicek ke kode). Frontend: method baru `cancelQueue()` (KEDUA varian widget) = `sessionClose()` + reset `@inQueue` + `showPrechatForm()` (cegah bug: tanpa ini, klik tab Messages lagi lewat tab bar akan menampilkan layar antre basi) + `switchTab('home')`
- [x] Diverifikasi Playwright: cancel tersedia di fase loader MAUPUN waiting, klik → pindah ke Home, DAN diuji khusus skenario balik ke tab Messages lewat tab bar setelah cancel → dikonfirmasi form pra-chat segar (bukan layar antre basi). Zero console error
- [x] **Koreksi user ("tab messages tidak sesuai mockup"): 1 bug NYATA parah + 2 gap struktur ditemukan & diperbaiki.** Bug: bubble pesan tampil 3x lebih tinggi dari seharusnya — root cause whitespace literal di `message.eco` dianggap baris kosong sungguhan oleh `white-space:pre-line` (bug LAMA, baru mencolok setelah padding diperbesar entri 118-121; `typingIndicator.eco` rentan bug sama, ikut diperbaiki). Gap 1: header tersambung agent sekarang punya avatar (foto/inisial)+dot status di pojok+nama+"Active now"+tombol Info (sesuai `Messages.dc.html`, header dilepas dari tinggi tetap 42px lama). Gap 2: avatar kecil per-bubble pesan (baru, helper `initialsOf()` dipakai ulang di header & bubble)
- [x] **1 bug laten ditemukan & diperbaiki SEBELUM jadi crash produksi**: varian non-jQuery (`chat-no-jquery.coffee`) sempat CRASH TOTAL saat buka widget gara-gara binding langsung ke elemen yang sekarang cuma ada setelah agent tersambung (`.js-chat-status` pindah ke `agent.eco`) — jQuery tidak crash (nge-mask bug yang sama). Ditemukan lewat pengujian KEDUA varian widget secara eksplisit (bukan cuma jQuery seperti kebiasaan), diperbaiki di keduanya lewat delegasi event
- [x] Diverifikasi Playwright genuinely lintas-domain di KEDUA varian widget: bubble 1 baris (~44px), header+avatar+info lengkap, avatar per-bubble benar warna & sisi, regresi Home dicek ulang, zero console error keduanya
- [x] **Koreksi user (screenshot beranotasi merah): 3 temuan terpisah diperbaiki.** (1) Ikon reply tampil kotak biru mencolok di browser sungguhan user (bukan CSS — karakter Unicode `&#8617;` kena presentasi emoji berwarna dari font Windows, mockup preview kebetulan tidak; diganti SVG, satu-satunya ikon widget yang masih pakai HTML entity bukan SVG); (2) tidak ada jam per pesan — ditambahkan (`created_at` asli dari server untuk pesan agent, TIDAK PERLU perubahan backend karena sudah ada di payload sejak awal, cuma belum dipakai frontend); (3) area mengetik pesan direstruktur total dari 1 baris jadi 2 baris (kotak input berbingkai sendiri + baris ikon di bawahnya dengan send jadi lingkaran penuh) sesuai `Messages.dc.html` — keputusan sadar untuk MENGIKUTI mockup halaman ini meski beda dari preseden `ChatSendMsg.vue` yang dipakai entri 121 (dicatat eksplisit)
- [x] Diverifikasi Playwright: ikon reply dikonfirmasi elemen `<svg>` sungguhan, jam tampil benar di semua bubble, emoji toggle dapat state aktif, regresi Home dicek ulang, zero console error
- [x] **Koreksi user (4 laporan terpisah dari pemakaian sungguhan): semua diperbaiki.** (1) Ikon reply muncul di HAMPIR SEMUA bubble termasuk pesan customer sendiri — bug LOGIKA (`@id` truthy, harusnya `@from is 'agent'`), bukan cuma visual; (2) placeholder "Compose your message..." TIDAK PERNAH tampil — `::-webkit-input-placeholder` ternyata dead code sejak lama (tidak berlaku utk `<div contenteditable>`), diganti `:empty::before`; (3) teks bubble tiba-tiba bold — perilaku native Zammad lama (`--unread`) tanpa padanan di mockup manapun, efek visual dihapus (logika/class tetap ada); (4) hard refresh menghilangkan avatar+jam SEMUA riwayat — root cause ganda: jalur `onReopenSession` (reconnect/replay) terlewat saat field itu ditambahkan sebelumnya, DAN `@customerName` cuma di memori (hilang saat reload, server tidak kirim balik) — diperbaiki dengan persist ke `sessionStorage` + lengkapi `onReopenSession`
- [x] Diverifikasi Playwright TERMASUK `page.reload()` sungguhan (bukan cuma buka widget baru): reply icon 0 di customer/3 di agent, placeholder terkonfirmasi via computed style, semua font-weight 400, avatar+jam SEMUA riwayat terisi benar setelah reload sungguhan. Regresi Home dicek ulang, zero console error
- [x] **Atas permintaan user untuk simulasikan sendiri (hard refresh lalu klik ikon reply): ditemukan 1 bug FUNGSIONAL nyata** — fitur reply diam-diam GAGAL TOTAL (tanpa error) untuk semua pesan hasil replay riwayat setelah reload, karena `onReopenSession` tidak pernah mengisi `@agentMessagesById` (cuma diisi `receiveMessage`, jalur real-time) yang dibutuhkan `startReply`. Diperbaiki di KEDUA varian widget — ini TIDAK akan pernah ketahuan dari cek visual/screenshot manapun, cuma dari simulasi klik sungguhan
- [x] **Ikon attachment "terlalu kontras"**: root cause blok CSS `.zammad-chat-attach` DUPLIKAT (kali ke-3, pola sama dgn bug tabbar entri 119 & attach entri 121) — `fill:currentColor` lama menimpa `fill:none` yg benar via urutan source, path stroke-only jadi terisi solid. Dihapus + komentar peringatan ditambahkan
- [x] Diverifikasi Playwright PERSIS skenario user: reload sungguhan → klik reply → indikator tampil dgn teks benar → kirim balasan → kutipan dikonfirmasi ada di pesan terkirim (fungsional end-to-end, bukan cuma UI). Ikon attach dikonfirmasi `fill:none` + stroke abu-abu. Zero console error
- [x] **Lanjutan simulasi reload atas permintaan user: kutipan reply ("Membalas: ...") hilang total setelah hard refresh — bug BACKEND** (bukan cuma frontend seperti 3 bug sebelumnya). Root cause: `Chat::Session.messages_by_session_id` (widget customer) DAN `active_chats_by_user_id` (agent/self-service Fase 6) sama-sama cuma kirim kolom mentah (`reply_to_id` angka), tidak pernah menyertakan isi pesan yang dikutip — pola enrichment yang sudah ada di jalur real-time (`chat_session_message.rb`, Fase 5) tidak pernah diterapkan ulang ke jalur riwayat/bulk ini. Diperbaiki di `app/models/chat/session.rb` (helper baru dipakai KEDUA method, sekaligus ikut membenahi jalur agent yang belum sempat dilaporkan) + deploy ke kedua container + restart websocket + frontend `onReopenSession` (kedua varian) diberi `replyTo`
- [x] Diverifikasi Playwright PERSIS skenario user: kirim+balas dgn kutip → reload sungguhan → kutipan dikonfirmasi tetap tampil sama (sebelumnya hilang total). Zero console error
- [x] **Koreksi user ("widget terlalu tinggi, header tidak nampak, buat proporsional")**: `height:640px` tetap (warisan mockup, dirancang di kanvas yg selalu cukup tinggi) tidak pernah mempertimbangkan jendela browser pendek — 640px panel + 92px jarak launcher butuh viewport min 732px, sering tidak terpenuhi. Diganti `height: min(640px, calc(100vh - 116px))` — tetap 640px persis di jendela cukup tinggi, otomatis mengecil proporsional (bukan terpotong) di jendela pendek. Aman krn konten tengah sudah `flex:1;overflow:auto` sejak awal, header/tabbar/kotak-ketik tidak ikut terpotong
- [x] Diverifikasi Playwright 2 skenario viewport (pendek 620px & tinggi 900px sbg regresi) — header selalu dikonfirmasi PROGRAMATIK utuh dalam area kelihatan, panel tetap 640px persis saat viewport cukup tinggi
- [x] **Atas permintaan user: ikon reply dipindah ke LUAR bubble** (mockup `Messages.dc.html` direvisi dulu, lalu dieksekusi ke widget). Struktur baru: `.zammad-chat-message-row` (flex) membungkus bubble + tombol reply sbg sibling, bukan lagi `position:absolute` menumpuk di pojok dalam bubble. Sempat menulis ulang blok CSS duplikat (ke-4 kalinya pola sama) — ketahuan & dibetulkan SEBELUM deploy lewat `grep` dulu (kebiasaan baru sejak entri 127)
- [x] Diverifikasi Playwright: posisi ikon dikonfirmasi programatik di LUAR tepi bubble (tidak overlap), fungsi reply tetap jalan end-to-end setelah restrukturisasi, regresi Home dicek ulang, zero console error
- [x] **"Kenapa attachment tidak ada reply?" + "logo Home dari Setting Zammad"** — keduanya diimplementasikan. Attachment: `attachment_message.eco` dirombak jadi struktur PERSIS `message.eco` (avatar+reply+jam, dulu bare-bones sejak dibuat), `@agentMessagesById` diisi juga utk attachment (dibutuhkan reply). Logo: backend (`chat_status_customer.rb`) sisipkan `logo_url` (dibangun dari `product_logo` Setting, path persis preseden `feedback_controller.rb`) ke payload yg sudah selalu terkirim; frontend ganti ikon generik Home jadi `<img>` nyata begitu payload tiba, fallback ikon lama tetap jalan kalau belum ada logo custom
- [x] **Bug LEBIH PARAH ditemukan lewat pengujian aktif (bukan laporan user)**: pesan attachment SETELAH hard refresh tampil sbg bubble teks "[attachment]" TANPA link unduh sama sekali (hilang akses ke file, bukan cuma kehilangan avatar/jam). Root cause: jalur riwayat/reconnect backend tidak bisa membedakan attachment dari teks biasa. Diperbaiki: `enrich_message_attributes` (backend, dipakai KEDUA jalur riwayat) sisipkan `filename` kalau ada `Store` terkait, frontend pakai ini sbg penanda utk render lewat `attachment_message.eco`
- [x] Diverifikasi Playwright: logo asli tampil (Setting sungguhan terisi di instance ini), attachment dapat avatar+reply+jam, reply-pada-attachment fungsional end-to-end, hard-refresh attachment dikonfirmasi link tetap valid. Regresi Home dicek ulang, zero console error
- [x] **Item terbuka dicek & DIKONFIRMASI BENAR**: sisi AGENT (`app/assets/javascripts/app/controllers/chat.coffee`, `App.ChatWindow`) punya bug attachment-hilang-setelah-reload yang SAMA PERSIS — jalur riwayat (`open()`) selalu pakai `@addMessage` (template teks) tanpa cek `filename`. Diperbaiki: cek `message.filename` (backend SUDAH siap sejak entri 131, tidak perlu ubah backend lagi) → `@addAttachmentMessage` kalau ada. Deploy BEDA dari widget (asset pipeline Sprockets, `config.assets.compile=false` dikonfirmasi langsung) — wajib `assets:precompile` + restart `zammad-app` (bukan websocket)
- [x] Diverifikasi Playwright PERSIS skenario dicurigai: agent reload halaman sendiri → link attachment dikonfirmasi programatik tetap valid (sebelumnya pasti gagal). Regresi TAMBAHAN krn deploy lebih besar (precompile+restart app penuh): login/Dashboard/Overviews/admin Customer Chat semua dicek tetap normal, zero console error
- [x] **Koreksi user ("seolah-olah widget itu transparent"): DIBUKTIKAN benar lewat pengujian aktif** (teks merah percobaan diposisikan di belakang header, screenshot menunjukkan tembus jelas; `getComputedStyle` dikonfirmasi `rgba(0,0,0,0)`). Root cause: `.zammad-chat` (panel ROOT) tidak pernah punya `background` sama sekali sejak restrukturisasi Fase 7 — semua mockup selalu eksplisit `background:#ffffff`, tidak pernah ikut disalin ke kode. Selama ini "tampak putih" cuma kebetulan halaman uji kosong di titik itu — paling kentara di header tint (didesain menumpuk di atas putih SOLID). Diperbaiki: `background: white` di root, satu sumber kebenaran utk seluruh panel
- [x] Diverifikasi Playwright: bleed test yang sama diulang setelah fix — teks merah tidak tembus sama sekali, `backgroundColor` dikonfirmasi `rgb(255,255,255)`. Regresi Home dicek ulang, zero console error
- [x] **Permintaan user: pisahkan total perilaku tombol X header ("exit") vs tombol mengambang/chevron ("minimize")** — sebelumnya berbagi 1 method (`close()`) yg SAMA-SAMA diam-diam mengakhiri sesi chat. `close()` (launcher) sekarang HANYA sembunyikan panel, TIDAK sentuh sesi sama sekali. Method baru `exitChat()` (khusus `.js-chat-close`) yg mengakhiri sesi (`sessionClose()`) lalu `switchTab('messages')` (BUKAN hide panel, sesuai diminta eksplisit)
- [x] **Bug turunan KEDUA ditemukan lewat pengujian sendiri**: `onCloseAnimationEnd()`'s `@io.reconnect()` (sebelumnya tak masalah krn sesi sudah dihapus duluan) sekarang legitimately memicu `onReopenSession()` yg PUNYA `@show();@open()` tanpa syarat (dirancang cuma utk reload halaman) — panel jadi terbuka paksa lagi sesaat setelah minimize. Diperbaiki flag baru `@minimizedWithSession`
- [x] **Bug turunan KETIGA ditemukan lewat pengujian Playwright tahap kedua**: guard yg dipasang di atas SALAH SASARAN ikut membungkus `@show()` — padahal itu yg mengembalikan visibilitas TOMBOL LAUNCHER ITU SENDIRI (dihapus `onWebSocketClose` di tengah siklus reconnect yg sama) — akibatnya launcher jadi tidak kelihatan/tidak bisa diklik lagi setelah minimize (tidak ada cara buka chat lagi). Diperbaiki: `@show()` dipanggil tanpa syarat, hanya `@open()`/`@scrollToBottom()` yg tetap dijaga guard
- [x] Diterapkan ke KEDUA varian widget (`chat.coffee` & `chat-no-jquery.coffee`) identik, termasuk kedua revisi bug turunan
- [x] Diverifikasi Playwright genuinely lintas-domain, skenario lengkap: minimize→panel tertutup TAPI sesi tetap hidup→buka lagi→kembali ke tab Messages dgn pesan lama utuh (bukan pra-chat baru)→klik exit→panel TETAP TERBUKA (bukan hide) di tab Messages dgn sesi benar2 berakhir. Regresi Home dicek ulang, zero console error
- [x] **Sempat diminta tambah tombol "End Chat" terpisah di header Messages (mockup) — dibatalkan/dihapus lagi oleh user tak lama setelah dipublikasikan.** Mockup `Messages.dc.html` dikembalikan ke versi tanpa tombol tersebut (Version 20)
- [x] **Permintaan user: "hanya tombol X pada halaman Messages yang bisa untuk end chat, tombol X pada halaman lain berfungsi normal"** — `exitChat()` (KEDUA varian widget) sekarang mengecek `@activeTab` (tombol X adalah 1 elemen tunggal dipakai semua tab, bukan per-tab): di tab Messages tetap mengakhiri sesi (perilaku entri 134), di tab lain (Home/Help) didelegasikan ke `close()` (cuma sembunyikan panel, sesi TIDAK disentuh)
- [x] Diverifikasi Playwright 3 skenario: X di Home sebelum ada sesi (tutup normal), X di Home DENGAN sesi aktif di background (tutup normal, sesi TETAP hidup, lanjut normal saat dibuka lagi), X di Messages (sesi benar2 berakhir, panel tetap terbuka). Zero console error, regresi Home dicek ulang
- [x] **Mockup baru "loading proses chat berakhir"** (`EndingChat.dc.html`, ditambahkan ke `canvas.json`) — overlay spinner merah "Mengakhiri percakapan…" di atas panel Messages, lalu dieksekusi ke widget sungguhan: klik X di tab Messages → tampilkan overlay ini (`views/ending_chat.eco`, reuse penuh struktur spinner `loader.eco`/`waiting.eco` + 2 modifier CSS baru `--danger`) → jeda 2 detik (`setTimeout`) → kembali normal ke tab Messages
- [x] **Ditemukan & dibetulkan sekalian**: `chat_session_close.rb` (backend) TIDAK PERNAH mengirim balik `chat_session_closed` ke customer yang menginisiasi sendiri (`send_to_recipients` mengecualikan pengirim) — transisi loading→selesai DIPASTIKAN murni lokal/timer, bukan menunggu server. Juga: `updateHeader()` di `exitChat()` SELALU no-op sejak entri 135 (switchTab jadi no-op krn tab tujuan sama dgn aktif) — header diam2 tetap tampilkan info agent lama setelah chat berakhir, dipanggil manual sekarang
- [x] Diterapkan ke KEDUA varian widget, diverifikasi Playwright: modal+spinner merah tampil segera, `sessionId` langsung `undefined`, modal dikonfirmasi masih ada di t+1.15s dan sudah hilang di ~2.6s, header dikonfirmasi TIDAK lagi tampilkan agent lama setelah reveal. Regresi Home & skenario X-per-tab (entri 135) diulang, zero console error
- [x] **Revisi lanjutan: tujuan setelah loading "mengakhiri percakapan" diubah dari "kembali ke halaman Messages" (entri 136) jadi "kembali ke halaman MULAI CHAT"** — `exitChat()` (KEDUA varian) sekarang memanggil `goToStartChat()` (`showPrechatForm()` + `switchTab('home')`, persis pola `cancelQueue()` yang sudah ada) setelah jeda 2 detik, bukan `hideModal()` diam di Messages
- [x] Diverifikasi Playwright: overlay loading dikonfirmasi masih tampil & tab masih Messages di t+1.15s; setelah ~2.6s tab aktif sudah `home` dgn form pra-chat siap di modal Messages; pindah manual ke tab Messages sesudahnya dikonfirmasi langsung menampilkan form segar (bukan riwayat lama). Regresi Home & X-per-tab diulang, zero console error
- [x] **Mockup "penanda terkirim/dibaca ala WhatsApp" di Messages** (centang 1 abu2 = terkirim, centang 2 biru = dibaca — 2 contoh pesan outgoing ditambahkan sekaligus utk tunjukkan kedua status), lalu diriset menyeluruh (backend TIDAK PUNYA infrastruktur ini sama sekali — dikonfirmasi lewat grep+schema check) dan dieksekusi penuh ke kode sungguhan
- [x] **Backend**: migrasi `read_at:datetime` di `chat_messages` + event WS baru `chat_session_message_read.rb` (pola sama `chat_session_typing.rb`, bulk "read up to now", cuma agent yang bisa memicu) — `read_at` otomatis ikut jalur riwayat tanpa kode enrichment tambahan
- [x] **Agent** (`app/assets/javascripts/app/controllers/chat.coffee`): `clearUnread()` yang SUDAH ADA (fokus/klik jendela chat) sekarang JUGA kirim event baru ke server, hanya kalau memang ada pesan belum terbaca
- [x] **Widget customer** (KEDUA varian): markup centang di `message.eco`+`attachment_message.eco` (khusus pesan customer), method `markMessagesRead()` update bulk saat event masuk, `onReopenSession` diberi `isRead` dari `message.read_at`, CSS baru `--sent`/`--read`
- [x] Diverifikasi Playwright end-to-end: kirim pesan → centang 1 abu2 segera; agent fokus jendela chat → centang 2 biru dlm ~1.2 detik; hard refresh → status tetap bertahan dari riwayat. Zero console error kedua sisi. Regresi Home, X-per-tab, loading-mengakhiri-chat, DAN regresi tambahan sisi agent (login/dashboard/admin Customer Chat, krn deploy precompile+restart app) semua diulang & lolos
- [x] **Koreksi user (screenshot): nama file attachment panjang bikin jendela chat scroll horizontal — minta di-truncate proporsional.** Root cause 3 lapis: `.zammad-chat-attachment` (inline-flex) jadi kotak atomik yg tidak tunduk `overflow-wrap` bubble; `white-space:nowrap` pada nama file bikin lebar-minimumnya = lebar teks utuh (ellipsis murni visual, tidak mengecilkan box); `.zammad-chat-message-row` tidak punya `min-width:0`/`max-width:100%` shg lebar raksasa itu menjalar ke SELURUH baris pesan
- [x] Perbaikan: `.zammad-chat-message-row` diberi `min-width:0`+`max-width:100%`, `.zammad-chat-attachment` diberi `max-width:100%`, `.zammad-chat-attachment-filename` diberi resep ellipsis standar — proporsional mengikuti sisa ruang, tidak ada angka piksel tetap
- [x] Diverifikasi Playwright programatik: sebelum fix `scrollWidth` 766px vs `clientWidth` 378px (overflow nyata); setelah fix scrollWidth=clientWidth di semua level DAN nama file dikonfirmasi terpotong beda lebar (agent vs customer, sesuai ruang tersedia masing2). Regresi: pesan teks pendek+ikon reply/status tetap natural, Home diulang, zero console error
- [x] **Koreksi user (screenshot): kutipan reply ke pesan attachment menampilkan literal "[attachment]", minta diganti nama file asli + di-truncate.** Backend: method baru `Chat::Message#display_content` (reuse pola deteksi `Store` yg sudah ada) dipakai di `chat_session_message.rb` (real-time) & `enrich_message_attributes` (riwayat). Frontend (KEDUA varian): `startReply()` pakai `message.filename || message.content`. CSS: `.zammad-chat-message-quote` diberi ellipsis standar
- [x] **Item dicek TAPI sengaja tidak diubah**: baris identik di sisi AGENT (`app/assets/javascripts/app/controllers/chat.coffee`) tidak pernah bisa tereksekusi dgn attachment (agent tidak py tombol reply pada attachment sama sekali) — tidak disentuh, konsisten dgn item terbuka sejak entri 131
- [x] Diverifikasi 2 lapis (upload file sungguhan lintas-context TERBUKTI flaky utk skenario ini): backend murni via `rails runner` (display_content benar utk attachment/reply_to/pesan biasa), frontend murni via injeksi+klik tombol reply asli (indikator & kutipan bubble terkirim dikonfirmasi nama file, terpotong, tanpa overflow). Regresi Home & truncation attachment (entri 139) diulang, zero console error
## Enhancement Baru (di luar Fase 1-7, direncanakan lewat mockup+diskusi urutan sebelum eksekusi)
- [x] **Enhancement 3 — Tahap 1: Logo tanpa teks, dari Setting** — `views/home.eco` span teks "SISKA" dihapus, `.zammad-chat-home-logo-mark` diperbesar (40px→52px) berdiri sendiri. `updateHomeLogo()` (entri 131) tidak perlu diubah. Diverifikasi Playwright — elemen teks dikonfirmasi hilang, logo ASLI (Setting `product_logo` yg sudah terisi di instance ini) dikonfirmasi tampil sendirian. Regresi Home disesuaikan (assersi lama diganti sesuai desain baru), zero console error
- [x] **Enhancement 1 — Tahap 2 (fondasi)**: ekstraksi logika buat-tiket dari `chat_session_start.rb` jadi `Chat::Session#create_ticket_for_chat!` (method publik, badan identik baris demi baris) — dipakai chat biasa sekarang, siap dipakai offline message di Tahap 3. Diverifikasi end-to-end sungguhan (tiket lahir benar: judul/grup/customer/artikel pertama sama persis) + guard idempotensi diuji eksplisit (panggil 2x, tidak bikin tiket ke-2). Regresi Home diulang, zero console error
- [x] **Enhancement 1 — Tahap 3**: Offline Message + Verifikasi OTP — LENGKAP (lihat sub-item di bawah)
  - [x] Deteksi "semua agent offline kecuali AUX" — `Chat.active_agent_count`/`active_agents` filter `aux_status`, reuse state `'offline'` yg sudah ada (tidak perlu state baru). Diverifikasi 5 skenario AUX end-to-end + regresi Home, zero console error
  - [x] Migrasi kolom OTP di `Chat::Session` (`otp_code` hash, `otp_expires_at`, `otp_verified_at`, `otp_attempts`) + 3 Setting fungsional (expiry/cooldown/max-attempts, area baru `SISKA::OfflineMessage`) — diverifikasi langsung ke server, kolom & Setting dikonfirmasi ada/benar di KEDUA container
  - [x] 4 event WS baru (`chat_offline_session_init`, `_otp_verify`, `_otp_resend`, `_message_send`) + validasi ulang OTP & "agent beneran offline" di server (bukan cuma percaya UI) + wiring email OTP (`Chat::Session#generate_and_send_otp!`/`verify_otp`, `NotificationFactory::Mailer.deliver` langsung)
  - [x] **Bug ditemukan & diperbaiki**: `create_ticket_for_chat!` gagal total (silent, tertelan rescue) utk visitor anonim -- `created_by_id`/`updated_by_id` Ticket WAJIB diisi eksplisit (`user_id.presence || 1`), sebelumnya mengandalkan `UserInfo` ambien yg cuma ada saat pemanggil agent login. Diverifikasi 8 skenario backend murni + regresi chat biasa diulang penuh (tidak berubah), zero console error
  - [x] **Frontend LENGKAP, kedua varian widget**: `enterOfflineMode()` (gantikan `@onError`+`@destroy` lama), reuse penuh form pra-chat, layar `OfflineOtp` (6 kotak digit + auto-lompat + backspace + paste) → `OfflineCompose` → `OfflineSent` → "Back to Home". 3 file eco baru + `home.eco`/`chat.scss` diperbarui
  - [x] **Bug ditemukan & diperbaiki**: 3 pemanggilan `@send` lupa `session_id` (ketahuan dari `chat_error` di console); ikon amplop tertimpa rule generik `fill:currentColor` (bug berulang ke-4, pola sama entri 121/127/130) -- diperbaiki selector 2-class tanpa `!important`
  - [x] **Scope disederhanakan (dicatat eksplisit)**: lampiran file di compose belum diimplementasikan; header Otp/Compose/Sent tetap judul generik "Messages"; tidak ada transisi otomatis kalau agent online di tengah alur; alur tidak bertahan lewat hard refresh
  - [x] Playwright E2E alur penuh (9 langkah) — KEDUA varian widget, lolos identik. Backend dikonfirmasi ulang (tiket+artikel benar). Cooldown resend diuji terpisah. Regresi penuh (chat biasa, X-per-tab, Home pixel) diulang, zero console error semua skenario
  - [ ] Admin UI utk mengubah default frase/Setting fungsional Enhancement 1 (belum ada, saat ini via Rails console/generic Setting API) -- bukan blocker, dicatat sbg follow-up
- [x] **Enhancement 2 — Tahap 4**: Rating Kepuasan (Feedback) — LENGKAP, terintegrasi LANGSUNG ke kolom CSAT Fase 1 (`csat_score`/`csat_comment`/`csat_submitted_at` di Ticket, BUKAN tabel baru)
  - [x] Event WS baru `chat_session_feedback_submit` — meniru persis pola tulis `FeedbackController#submit` (`update_columns` + validasi skor 1-5)
  - [x] Guard anti-survey-dobel: `.where(csat_submitted_at: nil)` disisipkan ke query `Service::Csat::PrepareFeedbackSurveys#send_surveys_for_newly_closed_tickets`
  - [x] Frontend LENGKAP, kedua varian widget: `exitChat()`'s closure lokal direfactor jadi method `goToStartChat()`; `@lastSessionId` menangkap sesi sebelum `@sessionId` dikosongkan; layar baru `views/feedback.eco` (5 bintang + komentar opsional) & `views/feedback_thanks.eco` (auto-redirect 2 detik); disisipkan setelah EndingChat (chat biasa) & setelah OfflineSent (tombolnya diganti teks "Continue", alur offline)
  - [x] **Bug LAMA ditemukan & diperbaiki (dampak besar ke Fase 1, bukan cuma Enhancement 2)**: `ObjectManager::Attribute` `csat_submitted_at`/`csat_email_sent_at` salah konfigurasi `diff: 0` (truthy di Ruby) → Zammad inti otomatis mengisi KEDUA field itu di SETIAP tiket baru sejak 16 Sep, bikin job survei CSAT Fase 1 macet total (filternya `csat_email_sent_at: nil` tidak pernah cocok) + mencemari data pelaporan. Diperbaiki `diff: nil` (pola sama field native `pending_time`) di `script/create_csat_object_attributes.rb`, 108 tiket lama dibersihkan (izin eksplisit user, hanya yg `csat_score` kosong yg disentuh)
  - [x] Playwright E2E kedua skenario (chat biasa + pesan offline) x kedua varian widget: submit rating, validasi skor kosong, skip "Maybe later", auto-redirect Thanks→Home, DB dikonfirmasi benar tiap kasus. Zero console error
- [x] **Enhancement 4 — Tahap 5**: Semua frase widget configurable — LENGKAP, 55 Setting baru (bukan ~35 spt estimasi awal, setelah cross-check ke kode sungguhan, bukan cuma mockup)
  - [x] 55 Setting baru (`script/create_widget_phrase_settings.rb`, area `SISKA::WidgetPhrases`), default = teks yang SUDAH LIVE (bahasa Inggris, keputusan eksplisit user -- BUKAN teks Indonesia di mockup, supaya "jadi configurable" tidak diam-diam ganti bahasa tampilan)
  - [x] `chat_status_customer.rb` diperluas: `widget_phrases` method + `phrases: {...}` di payload (pola sama dgn `logo_url`)
  - [x] Frontend LENGKAP, kedua varian widget: `@phrases`, `view()` menyisipkan ke semua template, `updatePhrases()` (pola sama `updateHomeLogo`), 15 file `.eco` + kedua `.coffee` diganti ke pola `@T(@phrases['key'] || 'default')`
  - [x] Dikecualikan dari scope (dicatat eksplisit): nama tab, aria-label ikon, pengumuman status sistem/koneksi, isi Help dari KB, `scrollHint` (sudah configurable sendiri), pesan error backend Ruby (`__()` di event handler offline) -- follow-up terpisah
  - [x] Playwright E2E dgn Setting BENAR-BENAR diubah ke nilai kustom (online + offline, sampai ke layar Feedback), kedua varian widget, zero console error. Regresi penuh diulang
  - [x] Follow-up: dukungan lampiran di OfflineCompose (item 3 dari 3 yg sempat ditunda) — `attachment_enabled?` mengizinkan sesi offline (saklar global saja, tanpa cek per-agent), `sync_attachment_to_ticket!`/`sync_pending_attachments_to_ticket!` (baru, dipanggil dari `create_ticket_for_chat!`) mensinkronkan RETROAKTIF attachment yg diupload SEBELUM tiket offline lahir. Reuse penuh endpoint `ChatAttachmentsController` Fase 5, TANPA endpoint baru. Playwright E2E upload file PNG asli, DB dikonfirmasi tiket dpt 2 artikel (teks+attachment), kedua varian widget
  - [ ] Item 1 (judul header per-layar Otp/Compose/Sent/Feedback) & item 4 (ikon di tombol kirim OfflineCompose) — TETAP DITUNDA (user pilih hanya item 3/lampiran utk dikerjakan sekarang)
  - [x] Bug: ikon peringatan di pesan error OTP tidak pernah ada (`.js-otp-error` cuma `<div>` teks polos, `.text()` akan menghapus ikon apa pun) — ditemukan user lewat perbandingan detail ke `OfflineOtp.dc.html`. Diperbaiki: ikon dipindah ke markup statis, `showOtpError` sekarang cuma menyasar `<span class="js-otp-error-text">` anak. Playwright E2E kedua varian, ikon dikonfirmasi tampil & tidak hilang setelah error berulang
  - [x] Bug REGRESI ditemukan user LANGSUNG setelah fix di atas: ikon tampil sendirian SEBELUM "Verify" diklik — `display: flex` (`.zammad-chat-offline-otp-error`) menang atas `display: none` (`.zammad-chat-is-hidden`) krn urutan baris di CSS (spesifisitas sama, bug LAMA yg baru ketahuan). Diperbaiki `:not(.zammad-chat-is-hidden)`. Diverifikasi `getComputedStyle` langsung (bukan cuma cek class)
  - [ ] Follow-up: audit sistemik pola `display: flex/block/grid` yg didefinisikan SETELAH `.zammad-chat-is-hidden` (baris 791) tanpa `:not(.zammad-chat-is-hidden)` — berpotensi bug SERUPA di elemen lain, belum diaudit penuh
  - [x] Bug ditemukan user lewat simulasi manual (Leave us a message → nama+email → OTP → hard refresh): visitor mendarat di tab Messages KOSONG (spt chat biasa nyangkut), bukan reset ke Home. Akar masalah: case `'offline'` di handler `chat_status_customer` tidak membersihkan `@sessionId` basi sesi `offline_pending` (beda dari case `'online'` yg sudah lebih dulu, walau via jalur tidak lengkap). Diperbaiki: kedua case sekarang pakai `setSessionId(undefined)` yang benar (bersihkan in-memory + `sessionStorage`), kedua varian widget. Playwright E2E ulangi skenario PERSIS yang diminta user + regresi reconnect sesi chat sungguhan (`verify_close_per_tab.js`) tetap lolos
  - [x] 3 gap styling OfflineCompose ditemukan user lewat perbandingan detail ke mockup, semua diperbaiki: badge "verified" jadi strip full-width (bukan teks polos), jarak label "Your message" ke textarea 22px (bukan 8px), chip lampiran jadi netral+ikon penjepit kertas (bukan hijau+centang, warnanya sebelumnya tanpa sadar disamakan dgn badge verified)
  - [x] Bug ditemukan user LANGSUNG setelahnya: label "Your message" rata tengah (warisan `text-align: center` dari `.zammad-chat-modal`, cocok utk layar status tapi TIDAK utk OfflineCompose yg bergaya form). Diperbaiki `text-align: left` di `.zammad-chat-offline-compose` (root, bukan ditambal per elemen). Judul header "Messages" TETAP center -- itu item TERPISAH (judul per-layar) yg sengaja ditunda, bukan bagian bug ini
  - [x] **Audit Able Pro** (subagent, laporan lengkap ke starter kit lokal): 2 temuan lintas-elemen (tombol CTA baru masih pola lama; banyak spacing bukan kelipatan 4px) + temuan detail per-layar (OfflineOtp/Compose/Sent, Feedback, FeedbackThanks, Home notice, EndingChat). Eksekusi bertahap per pilihan user:
    - [x] **Tahap 1 -- OfflineOtp**: kotak digit (radius→8px, height 50→46px, font 19px/700→13px/400, semua sesuai `AuthCodeVerification.vue`), tombol Verify disatukan ke selector CTA bersama yg sudah ada, link "Resend code" dapat state hover/padding (sebelumnya `<button>` polos tanpa state)
    - [x] **Tahap 2 -- OfflineCompose**: 2 bentrokan mockup-vs-Able-Pro ditanyakan ke user dulu (label margin 22px vs 8px, border badge netral vs senada) — user putuskan Able Pro menang, mockup diupdate ikut. Label margin 22px→8px, badge border→senada hijau, chip font 12.5px→14px. Mockup Artifact `OfflineOtp.dc.html` & `OfflineCompose.dc.html` disinkronkan ulang (Tahap 1 belum sempat)
    - [x] **Tahap 3 -- Home notice**: latar/border netral → tonal biru senada, radius 12px→8px (`$siska-radius-sm`, konsisten Able Pro utk SEMUA alert), ikon 18px→20px, padding/gap dirapikan ke kelipatan 4px. Prinsip "Able Pro menang" (disepakati entri 156) diterapkan tanpa tanya ulang. Mockup `OfflineHome.dc.html` disinkronkan
    - [x] **OfflineHome (di luar notice)**: TERNYATA bukan bug kode -- 2 mockup (`Home.dc.html` vs `OfflineHome.dc.html`) yg tidak konsisten satu sama lain soal urutan ikon-teks tombol. Kode sudah benar (konsisten dgn `Home.dc.html`). Diperbaiki HANYA di mockup `OfflineHome.dc.html` (urutan + pola tombol CTA yg ketinggalan)
    - [x] **Konsistensi Home vs OfflineHome**: dot status offline pakai literal mentah `#9aa5b1` (bukan token) — diperbaiki token baru `$siska-input-border` (`#bec8d0`, dicek ke `vuetify.ts`). Tipografi & gap lain sudah dikonfirmasi konsisten, tidak diubah
    - [ ] Tahap berikutnya (OfflineSent, Feedback, FeedbackThanks, EndingChat) — menunggu instruksi user
  - [x] **Bug arsitektur nyata ditemukan user via screenshot** (header online tapi notice+tombol offline MASIH tampil, mixed state) -- direproduksi Playwright dulu, terbukti nyata: WS reconnect (`onOpen: @render`, BUKAN cuma sekali di awal) memicu `chat_status_customer`/`updatePhrases` ulang, `enterOfflineMode()` versi lama cuma sanggup jalan SEKALI (`return if @offlineMode`) + `.zammad-chat-welcome-subtext` diganti TOTAL (`replaceWith`, elemen aslinya hilang selamanya) -- kombinasi ini bikin state campur aduk stlh reconnect PERTAMA saat agent msh offline. Diperbaiki: `enterOfflineMode()` disederhanakan, logika diekstrak ke `applyOfflineHomeState()` (BARU, aman dipanggil berulang tanpa batas, pakai `.html()`/`innerHTML` bukan `replaceWith()`), dipanggil dari `updatePhrases()` SETIAP KALI. Diverifikasi 3x siklus reconnect berturut-turut tetap konsisten, kedua varian widget, zero console error
  - [x] **Bug LANJUTAN ditemukan user LANGSUNG setelahnya**: "notice muncul padahal ada agent online" -- 2 akar masalah TERPISAH: (1) `@offlineMode` tidak pernah di-reset `false`, diperbaiki dihitung ULANG tiap status response LANGSUNG dari `pipe.data.state`, sebelum `updatePhrases`; (2) CSS spesifisitas SAMA PERSIS dgn bug ikon error OTP (entri sebelumnya) -- `.zammad-chat-home-offline-notice` py `display:flex` yg menang atas `display:none` `.zammad-chat-is-hidden` krn urutan baris, diperbaiki `:not(.zammad-chat-is-hidden)`. Audit sistemik 8 elemen lain yg jg toggle `zammad-chat-is-hidden` -- dikonfirmasi tidak ada yg bermasalah. Diverifikasi bolak-balik online<->offline 2x, kedua varian, zero console error
  - [x] Atas permintaan user, DIKOREKSI ke elemen yang benar (awalnya salah sasaran ke logo Home, DIBALIK): tombol **launcher** (ikon mengambang pojok kanan bawah, `@launcherEl` -- elemen terpisah, tidak pernah digambar ulang) jadi abu-abu saat offline, biru lagi saat online -- `@launcherEl?.toggleClass('zammad-chat-launcher--offline', @offlineMode)` di titik `@offlineMode` dihitung. Mockup `OfflineHome.dc.html` disinkronkan (lalu dikembalikan ke logo biru krn logo TIDAK jadi diubah)
  - [x] Atas permintaan user: pesan race-condition "An agent is available -- please use live chat instead." pakai gaya notice SUKSES Able Pro (tonal hijau + ikon centang), terpisah dari pesan gagal validasi (tetap merah) yg sebelumnya berbagi elemen sama. Backend `chat_offline_session_init.rb` ditambah `reason: 'agent_available'` sbg pembeda eksplisit (bukan cek isi teks). Param baru `notice` di `showPrechatForm`/`prechat.eco`, kedua varian widget
  - [x] **Atas permintaan user: SEMUA notice pada widget diubah dari style `tonal` ke style alert `outline` Able Pro.** Diriset dulu ke source Vuetify sungguhan (`_variant.sass`/`VAlert.css`, versi sama) — dikonfirmasi `outline` = background TRANSPARAN penuh (bukan tint) + border solid `currentColor`, teks/ikon/border satu warna semantik yg sama (beda mendasar dari `tonal`). 3 elemen dikonversi: `.zammad-chat-home-offline-notice` (biru), `.zammad-chat-prechat-notice` (hijau), `.zammad-chat-offline-compose-verified` (hijau, bentuk strip banner-nya TIDAK diubah, cuma treatment warnanya). Diverifikasi Playwright `getComputedStyle` (background transparan + border&teks 1 warna persis), kedua varian widget, zero console error. 3 mockup disinkronkan (`OfflineHome`, `OfflineCompose`, `Prechat` — yg terakhir sekalian ditambah notice "agent tersedia" yg belum pernah digambar di mockup-nya)
  - [x] Atas permintaan user (screenshot referensi Able Pro), KHUSUS notice Home offline: dibuat mengikuti PERSIS pola "prominent outlined" + `border="top"` + warna **warning** (oranye) Able Pro (`UiAlert.vue` baris 278, contoh lorem "Duis arcu tortor..."). Setelah 2x koreksi dari user (detail `border="top"` yg nambah aksen tebal 8px/opacity 0.38 khusus sisi atas, lalu warna warning bukan primary): ikon lingkaran padat 32px + simbol "!" putih, rata TENGAH thd teks (gap 12px), border tipis 3 sisi + aksen tebal pudar 1 sisi atas, warna teks/border/ikon semua `$siska-warning` (#e58a00). Mockup `OfflineHome.dc.html` disinkronkan
  - [x] **Bug PROSES DEPLOY ditemukan user ("sudah incognito masih tetap sama")**: halaman uji sungguhan (`test-chat-customer.html`) memuat `chat-no-jquery.**min**.js`, TAPI sepanjang entri 149-168 saya HANYA `docker cp` versi mentah (`chat.js`/`chat-no-jquery.js`) -- versi `.min.js` (hasil `uglify()` otomatis dari `npx gulp build`) TIDAK PERNAH ikut di-deploy sejak awal segmen ini, walau Playwright "lolos" tiap kali (krn skrip test SELALU load versi mentah, bukan `.min.js` spt user sungguhan). Diperbaiki: `chat.min.js`+`chat-no-jquery.min.js` (kumulatif SEMUA fix 149-168) di-`docker cp`, dikonfirmasi `curl`. **Konvensi deploy ke depan**: WAJIB ke-4 file (`chat.js`/`.min.js`, `chat-no-jquery.js`/`.min.js`) setiap kali `.coffee`/`.eco` berubah, tidak cukup 2 file mentah
  - [ ] Gap TAMBAHAN ditemukan dari perbandingan yg sama, BELUM dikerjakan (nunggu keputusan user): (a) subjudul OTP di mockup punya kalimat tambahan "Masukkan kodenya di bawah untuk melanjutkan." yg tidak ada di `chat_phrase_otp_subtitle_prefix` sekarang; (b) tombol resend mockup punya visual countdown "(0:45)" yg tidak pernah dibangun di frontend (cooldown-nya SUDAH ditegakkan backend, cuma tidak terlihat user); (c) pesan error OTP (kode salah/kedaluwarsa/terlalu banyak percobaan) masih hardcode `__()` di `chat_offline_otp_verify.rb` (backend Ruby), belum configurable spt frasa widget lainnya
  - [x] **Atas permintaan user ("semua element widget WAJIB pakai style Able Pro"): audit KOMPREHENSIF baru (7 kategori: Icon, Button, Input/Textarea, Border&Radius, Color, Font, Lainnya) atas SELURUH `chat.scss`+24 file `.eco`** — laporan ditunjukkan dulu ke user, dieksekusi bertahap sesuai pilihan user:
    - [x] **Kategori 1 -- Icon**: stroke-width diseragamkan ke `1.8` di SELURUH widget (sebelumnya tersebar 1.6/1.8/2/2.2 tanpa sistem, termasuk inline svg di `.coffee`); header icon info/close disamakan 17px; launcher icon open/close disamakan 24px; ikon submit `prechat.eco` disinkronkan ke nilai yg SUNGGUHAN dirender CSS (16px markup mati -> 18px); ikon attach (paperclip) disatukan ke 16px di SEMUA 3 tempat pemakaian (toolbar chat, tombol OfflineCompose, chip lampiran dinamis) + ikon emoji ikut disamakan (sepasang di baris sama). Sekalian: ikon send `fill="#ffffff"` literal -> `currentColor`. Diverifikasi Playwright thd `.min.js` (bukan versi mentah, sesuai pelajaran entri 169) + regresi `verify_close_per_tab.js`, zero console error. Mockup TIDAK disinkronkan (perubahan sub-piksel, tidak kasat mata di skala preview)
    - [x] **Atas permintaan user ("wajib pakai sprite SVG Able Pro"): SEMUA ~30 ikon widget diganti path SVG ASLI dari sprite Able Pro** (`public/assets/svg/sprite.svg`, 305 ikon), di-INLINE ke markup `.eco` (BUKAN `<use xlink:href>` — riset dulu: mekanisme itu tidak aman cross-origin utk widget yg di-embed di website customer, user pilih opsi inline yg aman). Dipetakan semantik: message-outline (bubble chat, branding), mail-outline (konteks email), support (tab Help), send-outline (tombol kirim), warning-fill/-outline (notice offline & error OTP), check-circle-fill (sukses besar), check polos (badge kecil), paper-clip (attach, disatukan 1 sumber), star-outline (rating), chevron, search, document, reply, logout. CSS disesuaikan di 6 lokasi mengikuti teknik fill/stroke asli tiap ikon (Able Pro campur keduanya). 2 ikon sengaja dipertahankan (read-receipt & scroll-hint, tidak ada padanan di sprite). Diverifikasi Playwright ke ~15 layar + regresi fungsional PENUH (`verify_close_per_tab.js`, `verify_offline_attachment_e2e.js` end-to-end asli, `verify_widget_phrases_offline_e2e.js`) — semua lolos, zero console error, kedua varian widget
    - [x] **Kategori 2 -- Button**: hover state ditambahkan ke SEMUA tombol yg sebelumnya tanpa hover (CTA bersama, header-icon, kb-result-link, feedback-skip/-submit — `--secondary` otomatis ikut krn modifier class). 4 tombol disatukan ke pola CTA (44px/16px/400/capitalize): `offline-compose-submit`/`offline-sent-button` digabung PENUH ke selector CTA bersama, `feedback-skip`/`feedback-submit` disamakan typography-nya saja (tetap `flex:1` berdampingan, warna tidak diubah). Opacity disabled disatukan ke `0.26` (nilai asli `VBtn.css` Vuetify, dicek langsung — bukan tebak salah satu dari 2 nilai lama). Sekalian ditemukan bug laten: `.zammad-chat-header-icon-close` py rule CSS terpisah yg menimpa balik ukuran ke 16px (bikin fix 16->17px entri 170 tidak pernah benar2 tampil) — dihapus. **Verifikasi mulai entri ini TANPA Playwright** (dihentikan sementara atas instruksi user) — pakai `curl` langsung ke CSS ter-deploy
    - [x] **Kategori 3 -- Input & Textarea**: fokus `<textarea>` (`offline-compose-textarea`, `feedback-textarea`) sebelumnya cuma ganti warna border (1px tetap) — disamakan ke pola SEMUA kontrol lain (border menebal 1px->2px + padding dikompensasi -1px). Dicek OTP-digit & help-search: sudah punya affordance sendiri yg cukup / sudah benar, tidak diubah
    - [ ] Kategori 4-7 (Border&Radius, Color, Font/Typography, Lainnya) — menunggu giliran user

## Item lama yang masih terbuka
- [ ] Audit yang SAMA utk sisi AGENT (halaman chat, settings, riwayat) — MASIH MOCKUP-ONLY, di luar scope "widget" yang barusan diselesaikan (widget = customer-facing saja)
- [ ] Eksekusi sisi AGENT (halaman chat, settings, riwayat chat) & halaman user-login ke kode sungguhan — masih mockup Artifact saja, belum dieksekusi
- [ ] `chat_auto_ticket_group_id` masih mengarah ke grup testing "QA - Internal Testing" (id 89) — WAJIB diarahkan ke grup produksi sungguhan sebelum go-live (dikonfirmasi ulang langsung ke server saat verifikasi mekanisme buat-tiket)
- [ ] Commit ke git — menunggu konfirmasi eksplisit user (belum diminta)
